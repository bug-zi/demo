# 优化建议

> 对现有功能的改进想法。想到就写一条，小优化直接让 AI 开发。
> 条目完成后（原始描述+完成说明）移入下方归档区顶部，格式 `### YYMMDD ｜ 标题`。

## 待完成

（空）

## 归档区（从新到旧）

### 260911 ｜ 【A3】报告图传播化重做：金句问答对 + 人设头像 + 扫码二维码

- 原始描述：【A3】报告图传播化重做：按「脱离网页仍能看懂」标准——对手、结果、金句、称号、二维码（二维码依赖 9.11 晚部署出的正式链接）
- 完成说明：全部落地。brainstorming 四问定案（金句=最痛一击问答对 / QR 编码 gang.debugzi.com / 固定品牌传播色 / 引入 qrcode-generator 而非手写编码器）。新文件 `src/lib/share-card.js`：`pickGoldenQuote` 方向感知选句（点火取最大 delta、灭火取最负——存档 delta 已翻转，方向即胜负方向；纯斗图/沉默回合不参选；无候选走兜底文案「一场寂静的较量，没有一句话落在点上。」）；版面 720×1000×2：红条→品牌行→结果大字→对手行（SVG path 以 Path2D 重绘的头像圆）→金句卡（琥珀标签+你/TA 双气泡，2 行截断）→称号卡→统计 2×2→底栏扫码来战+右下二维码（ECC M 白底黑码）；main.js `exportCard` 退化薄壳。配色写死品牌传播色（深夜底+怒气红+琥珀金）不读会话令牌——传播资产要四态一致，写死色只进 Canvas 不进 UI 令牌体系。验证：TDD 红（module not found）→绿；四套检查全绿（smoke 新增第 7 节：方向感知/斗图沉默排除/空局兜底/标签文案/QR 矩阵 finder pattern 与确定性）；真浏览器真引擎打局出图，像素色带采样（顶红条满宽、卡片、QR 白底黑码、兜底版无标签无气泡），期间发现并修掉头像泼墨 bug——自托管图标是 viewBox="0 -960 960 960" 的 960 系而非 24 系，按 24 硬算放大 40 倍泼出圆外，改按 SVG viewBox 解析坐标；jsQR 解码导出 PNG 的二维码内容确认为 https://gang.debugzi.com

### 260911 ｜ 【A2】情绪演出打磨：阶段切换动效 + 每人设专属破防演出

- 原始描述：情绪演出打磨：阶段切换动效 + 每人设专属破防演出（计划书 §5.3：退出群聊/已读不回/收摊了，随 A1 扩至 5 人设）（时间盒 4h 供开发者参考；专属演出做不完时是否降级为通用演出由开发者拍板）
- 完成说明：全部落地，专属演出没有降级——5 人设各配一种 finale.kind，降级路径保留给「未配 finale 的人设」（灭火局人设上线时自动走通用演出）。①阶段切换：跨阶段回合触发报幕行（文字通道，reduced-motion 后仍可感知）+ 怒气条闪红 + 头像抖动 + 阶段胶囊弹跳，对手气泡底色随 .log[data-stage] 越染越红（color-mix 纯令牌）。②专属破防演出：网友连发 6 条+「已开启好友验证」/亲戚头像全灰+「已退出群聊」/老板「你先好好想想」后撤回/摊主报幕后灯熄整屏压暗+「收摊了」/甲方「正在输入…」熄灭+「已读不回」。③玩家败北：自己气泡灰掉+「你说不出话了」。顺手修掉旧 finish 的「演出窗口内离场被硬拽进报告」问题（现在演出每步查屏、离场不追画）。验证：TDD 红→绿；smoke/dom-check/build/library-check 四套全绿（dom-check 新增第 16 节：三动效类/ai-gone 捕抓/rapid 连发/败北灰泡）；真浏览器四态令牌采样（发现并修掉 blossom 浅色报幕行对比度问题：阶段色 62% 混 --text 自适应）+ PowerShell 像素采样复核三处演出态。

### 260911 ｜ 计时优化：回合时限可选（默认不限时）+ 暂停按钮

- 原始描述（待完成两条）：①把对局的 30s 时间限制去掉改成无限时间（或者放到设置中让用户自行定义，默认设成不限时长）；②添加暂停按钮组件——如果有计时的话，点击暂停冻结时间。
- 完成说明：全部落地。设置弹窗新增「对局」区四档（不限时/15s/30s/60s，默认不限时，点击即时落盘、下一局生效，不受「清除」影响）；新文件 `src/lib/duel-options.js`（键 `gang-ai:round-seconds:v1`，防御姿势照 settings.js）；`createDuel` 加 `{ roundSeconds }` 选项、时限随局定格（进行中的局不随设置中途变卦）；选了时限的局 meta 区有倒计时 + 文字暂停按钮「暂停⇄继续」——手动暂停跨大厅往返保持、关设置弹窗不偷暂停、新回合自动解除；不限时的局无计时无暂停（超时沉默回合随之仅存在于计时局，README/CLAUDE.md 已写明）。TDD 红绿：smoke +引擎契约断言、dom-check 默认局无计时断言 + 9b 计时局小节（起跳/冻结/走秒/弹窗/往返）+ 设置档位断言；smoke/dom-check/build/library-check 四套全绿；真浏览器令牌采样验证 `.pause-btn`/`.round-seg-btn` 浅深两态纯令牌（过渡动画污染读数用 transition:none 注入排除，同先例）。

### 第1轮（260910）｜样式优化：双主题 + 背景图 + Material Symbols 图标体系

- 原始描述（design/样式改版/design.md 四条）：①浅色樱花粉/深色宝蓝双主题即时切换；②bg-light/bg-dark 背景图铺满 + 半透明卡片叠加；③全 App 禁 emoji，统一 Material Symbols Outlined；④组件禁彩亮色、只用主题色系。
- 完成说明：全部落地。经浏览器 mockup 三轮选型定关键决策（切换入口=顶栏+设置弹窗双入口；语义色=主题色+暖红双系 C 方案；头像=MS 圆底图标），实施 9 任务：theme.js 状态机 + styles.css 令牌化重写 + 12 个自托管 SVG 图标（CSS mask 渲染）+ 头像/星级/判定标签图标化 + 怒气条 data-stage 化 + 战绩图 Canvas 跟主题。smoke/dom-check 全绿（新增主题切换断言），build 通过，App 内 emoji 清零。spec/plan 已归档至 design/样式改版/archive/，现状回写 project/视听与界面.md。同日修复一轮：静态资源迁 public/assets/ + CSS 绝对路径（ 引用的 CSS 不在 Vite 模块图、自定义属性里的 url() 不被重写，此前背景图/图标实际 404），补 network+像素双重验证。
