# 问题与疑惑

> 存疑的行为、不确定的设计、想跟 AI 对齐的问题。想到就写。
> 澄清或解决后条目（原始描述+结论）移入下方归档区顶部，格式 `### YYMMDD ｜ 标题`。

## 待完成

（暂无）

## 归档区（从新到旧）

### 260911 ｜ 不限时局发完一言后剩余回合连环弹（沉默）

- 原始描述：当前在对线房对线时，每局游戏有8轮对话，但是在我发完第一次之后，后面7轮直接弹（沉默），正常应该是等我输入内容才对。请你查明问题并修复
- 结论：根因是 d8a92b0（计时优化）把「0=不限时」设为默认后，`startTimer()`/`resumeTimer()` 两个武装倒计时的入口漏加 `roundSeconds > 0` 守卫——0 秒起跳的 interval 会在 1 秒后判超时，`submitTurn('',{timeout:true})` 弹出（沉默）回合，该回合完成又重新武装计时器，级联烧掉剩余轮次（关设置弹窗的自动恢复是第二触发路径）。dom-check 此前没抓到：脚本 1200ms 的点击节奏快于幽灵周期（本地生成 350–800ms + 1000ms 间隔），busy 守卫恰好挡住；真人打字间隔远超 1.4s 必触发。修复：`src/main.js` 两处入口加早退守卫（与 setPaused/viewDuel 既有风格一致）。回归：dom-check 新增第 14 节（干等/关弹窗都不弹沉默、轮次不推进、输入框可用），红→绿，另做撤守卫复红的反向验证；smoke/library-check 零回归。附带发现（未动，待定夺）：回合生成中回对线房，该回合的我方气泡不重建，只补画 AI 侧
