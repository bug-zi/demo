# 人设系统

> 对应源码：`src/data/personas.js`、`src/data/categories.js` ｜ 数值结算规则见 [对线规则.md](对线规则.md)

## 现有人设（3 个）

| id | 名字 | 房间 | 头像 | 难度 | 招牌台词 | 软肋（delta） |
|---|---|---|---|---|---|---|
| wangyou | 杠精网友 | 杠精房（点火） | ⌨️ | ★3 | 「有一说一，你这话逻辑有问题」 | 不接招 28 / 截图挂他 32 / 反指出逻辑错误 30 |
| qinqi | 阴阳怪气亲戚 | 杠精房（点火） | 🍊 | ★2 | 「哎哟，还在北京呢？」 | 反问她孩子 32 / 塞红包提钱 28 / 请她示范 30 |
| laoban | 画饼老板 | 谈判房（点火） | 🍪 | ★4 | 「年轻人不要只看钱」 | 追问加班费 30 / 请他示范 34 / 追问股权 30 |

## 人设字段结构

```text
persona = {
  id, name, category('gang'|'deal'|'eq'), avatar, difficulty(1-5), tagline,  // 身份层
  intro, opener,                                      // 出场：人设简介 / 第一句话
  presets: [3 条预设话术],                             // 对线屏按钮，点一次作废一次
  softspots: [ { key, label, delta, keywords[] } ],   // 软肋层，3 个（灭火局语义=心结）
  stages:      { polite: [3], sarcastic: [3], agitated: [3] },  // 阶段台词
  softspotReactions: { [softspotKey]: [3 条] },       // 软肋被戳中的专属反应
  breakdown:   [3 句破防演出，逐条弹出],
}
```

- `category` 归房：`gang` 杠精房 / `deal` 谈判房（fire 点火局），`eq` 情商房（extinguish 灭火局）；未标注兜底归杠精房。分类登记表在 `categories.js`（房名/模式/锁定态）

- `keywords` 是**子串匹配**：玩家输入（去空白后）包含任一关键词即判命中；本地引擎用它判定，远程模式下它作为示例写进提示词，模型自行判断、本地只兜底
- 回复台词的选择优先级（llm.js localTurn）：自爆反应 > 软肋专属反应 > 破防台词 > 当前阶段台词 > 通用兜底

## 软肋数值设计（为什么是 ≈90）

- 每人设 3 个软肋 delta 之和 88–94：三软肋全中 + 补一句有效输出（+12）即破百
- 光靠有效输出：8 轮 × 12 = 96 < 100——**不找软肋就赢不了**，这是玩法核心约束
- 同一软肋第二次命中 delta 减半（防按着预设连点）

## 新增人设检查单

1. 在 `PERSONAS` 追加完整字段并挂 `category`（杠精/谈判=fire；情商=extinguish，软肋语义写「心结」）；三软肋 delta 之和控制在 85–95
2. 三个阶段各 ≥3 条台词、每个软肋 ≥3 条专属反应、breakdown 3 句
3. keywords 覆盖玩家自然说法的变体（您/你、口语词）；不写脏话与真实群体指向
4. `npm run smoke` 通过（脚本覆盖软肋命中路径与灭火局反向判定）
