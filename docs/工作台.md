# 工作台

> 开发者与 AI 的协作入口。想到什么，写进对应小节待完成区即可。
> AI 每完成一轮工作，把该轮条目（含原始描述与完成说明）移入归档区顶部「第N轮」小节。
>
> 分流规则：小功能直接写「新功能」小节让 AI 开发；大功能先在 `design/idea/` 起一页纸想法，说「立项」后升级为正式模块（见 [design/README.md](design/README.md)）。

## 优化建议（待完成）

## 新功能（待完成）

## 问题与疑惑（待完成）

## 归档区（第N轮，从新到旧）

### 第2轮（260910）｜皮肤系统：双皮肤四态 + 顶栏弹层切换

- 原始描述：保留现版「樱花/宝蓝」风格，恢复原初「深夜聊天窗」风格并补齐浅色模式（原版仅深色）；导航栏加「皮肤选择」组件供玩家切换，皮肤与深浅解耦（先选皮肤，每套皮肤内再切深浅）。
- 完成说明：全部落地。经浏览器候选稿选型（入口=顶栏弹层皮肤卡；白日配色=暖米纸），实施 6 任务：theme.js 皮肤 API（`gang-ai:skin`，与 data-theme 正交）+ skins.js 登记表 + styles.css 四态令牌块（块序即优先级，midnight 深色为原版 1:1、浅色白日·暖米纸为新设计，body 关背景图 + stage-pill 回素底彩字）+ 顶栏 palette 按钮与防闪脚本 + skin-popover.js 弹层（挂 body、对线中可开不暂停计时、点卡即切不关层）+ palette/check 自托管图标（14 个）。dom-check 第 7 节新增皮肤断言，smoke/build 全绿，dev 四态 computed token + 像素采样吻合，reload 验证持久化与防闪。spec/plan 归档至 design/样式改版/archive/，现状回写 project/视听与界面.md。

### 第1轮（260910）｜样式优化：双主题 + 背景图 + Material Symbols 图标体系

- 原始描述（design/样式改版/design.md 四条）：①浅色樱花粉/深色宝蓝双主题即时切换；②bg-light/bg-dark 背景图铺满 + 半透明卡片叠加；③全 App 禁 emoji，统一 Material Symbols Outlined；④组件禁彩亮色、只用主题色系。
- 完成说明：全部落地。经浏览器 mockup 三轮选型定关键决策（切换入口=顶栏+设置弹窗双入口；语义色=主题色+暖红双系 C 方案；头像=MS 圆底图标），实施 9 任务：theme.js 状态机 + styles.css 令牌化重写 + 12 个自托管 SVG 图标（CSS mask 渲染）+ 头像/星级/判定标签图标化 + 怒气条 data-stage 化 + 战绩图 Canvas 跟主题。smoke/dom-check 全绿（新增主题切换断言），build 通过，App 内 emoji 清零。spec/plan 已归档至 design/样式改版/archive/，现状回写 project/视听与界面.md。同日修复一轮：静态资源迁 public/assets/ + CSS 绝对路径（<link> 引用的 CSS 不在 Vite 模块图、自定义属性里的 url() 不被重写，此前背景图/图标实际 404），补 network+像素双重验证。
