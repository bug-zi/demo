# 样式优化实施计划（双主题 + 背景图 + Material Symbols 图标）

**Spec:** `docs/design/样式改版/archive/260910-样式优化设计方案.md`

> 状态：已归档——260910 随「样式优化」落地，9 个任务全部执行完毕。
> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 `docs/draft/260910-样式优化设计方案.md`（已批准 spec）把 GANG.AI 从单一深夜主题改造为「樱花粉浅色 / 宝蓝深色」双主题 + 背景图 + 自托管 Material Symbols 图标体系。

**Architecture:** `<html data-theme>` + CSS 变量令牌制（颜色全部经令牌，两主题各一套值）；`src/lib/theme.js` 管主题状态与 localStorage 记忆；图标用自托管 SVG + CSS mask + currentColor；战报 Canvas 用 `getComputedStyle` 读令牌跟主题。

**Tech Stack:** Vite + 原生 ES Modules，零新增运行时依赖（图标 SVG 拷贝进仓库）。

**本仓库约定（执行者必读）：**
- Git 操作全部由**用户手动完成**——每个任务末尾「请用户提交」步骤只给出建议命令，不要自己执行。
- 没有单测框架；`npm run dom-check`（jsdom 驱动真实入口）就是 TDD 载体：先加断言看它红，再实现看它绿。
- 文本一律 textContent 落地（`h()` 帮助函数，`src/lib/dom.js`），绝不拼 HTML；图标 span 是静态可信标记，用类名渲染。
- 验证命令：`npm run smoke`（引擎无头）、`npm run dom-check`（UI 流程）、`npm run dev`（人工目验，http://localhost:5173）。

---

### Task 1: 素材入库（背景 WebP + 12 个 SVG 图标）

**Files:**
- Create: `src/assets/bg-light.webp`、`src/assets/bg-dark.webp`
- Create: `src/assets/icons/{settings,volume_up,volume_off,light_mode,dark_mode,close,star,target,bolt,keyboard,theater_comedy,cake}.svg`（12 个）

- [ ] **Step 1.1: 下载图标包并拷贝 12 个 SVG 进仓库**

```bash
PROJ="/d/Code/黑客松/260909未央黑客松"
TMP="$TEMP/ms-plan-icons"; rm -rf "$TMP"; mkdir -p "$TMP/pkg" && cd "$TMP"
npm pack @material-symbols/svg-400 --silent
tar -xzf material-symbols-svg-400-*.tgz -C pkg
mkdir -p "$PROJ/src/assets/icons"
for i in settings volume_up volume_off light_mode dark_mode close star target bolt keyboard theater_comedy cake; do
  cp "pkg/package/outlined/$i.svg" "$PROJ/src/assets/icons/$i.svg"
done
```

- [ ] **Step 1.2: 验证图标就位**

Run: `ls "$PROJ/src/assets/icons" | wc -l` → Expected: `12`

- [ ] **Step 1.3: 背景图转 WebP（现 PNG 共 4MB，目标各 ≤300KB）**

```bash
python - << 'EOF'
from PIL import Image
import os
base = r'D:\Code\黑客松\260909未央黑客松'
for src, dst in [('resources/bg-light.png', 'src/assets/bg-light.webp'),
                 ('resources/bg-dark.png', 'src/assets/bg-dark.webp')]:
    im = Image.open(os.path.join(base, src)).convert('RGB')
    out = os.path.join(base, dst)
    im.save(out, 'WEBP', quality=82, method=6)
    print(dst, os.path.getsize(out) // 1024, 'KB')
EOF
```

Expected: 两行输出，各 ≤ 300KB。若某张超了，把 quality 降到 75 重跑。

- [ ] **Step 1.4: 跑回归确认无破坏**

Run: `npm run smoke && npm run dom-check`（在项目根目录）→ Expected: 两个脚本全部通过（纯新增资源，不影响行为）。

- [ ] **Step 1.5: 请用户提交**

建议：`git add src/assets/ && git commit -m "chore: 入库背景图 WebP 与 Material Symbols 图标素材"`

---

### Task 2: dom-check 增加主题断言（先红）

**Files:**
- Modify: `scripts/dom-check.mjs`（文件末尾追加）

- [ ] **Step 2.1: 在 `console.log('\n全部通过 ✅')` 之前插入主题测试段**

```js
/* ------------------------------------------------------------------ */
/* 6. 主题：顶栏切换 + localStorage 记忆                                */
/* ------------------------------------------------------------------ */

const themeBtn = $('#theme-toggle');
if (!themeBtn) fail('顶栏没有主题按钮');
const themeBefore = document.documentElement.dataset.theme;
if (themeBefore !== 'light' && themeBefore !== 'dark') {
  fail(`初始主题应为 light/dark，实际「${themeBefore}」`);
}
themeBtn.click();
const themeAfter = document.documentElement.dataset.theme;
if (themeAfter === themeBefore) fail('点击主题按钮后 data-theme 应该翻转');
if (window.localStorage.getItem('gang-ai:theme') !== themeAfter) {
  fail('主题选择应写入 localStorage');
}
themeBtn.click();
if (document.documentElement.dataset.theme !== themeBefore) fail('再点一次应切回原主题');
console.log('✓ 主题切换:', themeBefore, '→', themeAfter, '→', themeBefore);
```

- [ ] **Step 2.2: 跑测试确认红**

Run: `npm run dom-check`
Expected: FAIL `✗ 顶栏没有主题按钮`（前面的段全部 ✓ 后退出）

---

### Task 3: theme.js + 顶栏改造（按钮接线与图标两态）

**Files:**
- Create: `src/lib/theme.js`
- Modify: `index.html`（整文件替换）
- Modify: `src/main.js`（imports、wireTopbar、启动三行）

- [ ] **Step 3.1: 新建 `src/lib/theme.js`**

```js
/**
 * 主题（浅色樱花粉 / 深色宝蓝）。
 * 首次访问跟随系统偏好，手动切换后写入 localStorage 记忆。
 * 颜色全部落在 styles.css 的 [data-theme] 令牌里，这里只管状态。
 */

const KEY = 'gang-ai:theme';
const listeners = new Set();

export function getTheme() {
  const saved = localStorage.getItem(KEY);
  if (saved === 'light' || saved === 'dark') return saved;
  // jsdom 没有 matchMedia，别让测试环境炸
  if (typeof window.matchMedia === 'function' &&
      window.matchMedia('(prefers-color-scheme: dark)').matches) return 'dark';
  return 'light';
}

export function applyTheme(theme) {
  if (theme !== 'light' && theme !== 'dark') return;
  document.documentElement.dataset.theme = theme;
  for (const fn of listeners) fn(theme);
}

export function toggleTheme() {
  const next = getTheme() === 'light' ? 'dark' : 'light';
  localStorage.setItem(KEY, next);
  applyTheme(next);
}

/** 订阅主题变化（订阅时立即回调一次当前值），返回取消订阅函数。 */
export function onTheme(fn) {
  listeners.add(fn);
  fn(getTheme());
  return () => listeners.delete(fn);
}

export function initTheme() {
  applyTheme(getTheme());
}
```

- [ ] **Step 3.2: 整文件替换 `index.html`**

变更点：head 加防首帧闪色内联脚本；顶栏新增 `#theme-toggle`；⚙️/🔇 emoji 换成图标 span（图标 CSS 在 Task 4 落地前视觉上暂不可见，属预期中间态）。

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>杠精陪练房 · GANG.AI</title>
    <link rel="stylesheet" href="/src/styles.css" />
    <script>
      // 防首帧闪错色：样式渲染前先定主题（键与 src/lib/theme.js 保持一致）
      try {
        var t = localStorage.getItem('gang-ai:theme');
        if (t !== 'light' && t !== 'dark') {
          t = window.matchMedia && matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
        }
        document.documentElement.dataset.theme = t;
      } catch (e) {}
    </script>
  </head>
  <body>
    <div id="app">
      <header class="topbar">
        <div class="brand">
          杠精陪练房<span class="brand-en">GANG.AI</span>
        </div>
        <div class="topbar-right">
          <span id="engine-badge" class="badge">本地引擎</span>
          <button id="theme-toggle" class="ghost-btn" type="button" aria-label="切换深浅主题"></button>
          <button id="settings-btn" class="ghost-btn" type="button" aria-label="接入你的 AI">
            <span class="icon i-settings" aria-hidden="true"></span>
          </button>
          <button id="sound-toggle" class="ghost-btn" type="button" aria-pressed="false">
            <span class="icon i-volume_off" aria-hidden="true"></span><span>声音</span>
          </button>
        </div>
      </header>
      <main id="screen" class="screen" aria-live="polite"></main>
    </div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

- [ ] **Step 3.3: 改 `src/main.js` —— imports 加一行**

在 `import { openSettings } from './ui/settings-dialog.js';` 之后加：

```js
import { initTheme, toggleTheme, onTheme } from './lib/theme.js';
```

- [ ] **Step 3.4: 改 `src/main.js` —— 整函数替换 `wireTopbar()`，文件末尾启动序列改为三行**

```js
function wireTopbar() {
  repaintEngine();

  // 注意：这段必须在下面 sound-toggle 的早退之前，否则声音按钮缺失会连带跳过设置按钮
  document.getElementById('settings-btn')?.addEventListener('click', openSettingsDialog);

  const themeBtn = document.getElementById('theme-toggle');
  themeBtn?.addEventListener('click', toggleTheme);
  onTheme((theme) => {
    if (!themeBtn) return;
    // 浅色时显示月亮（下一步去深色），深色时显示太阳
    themeBtn.replaceChildren(
      h('span', {
        class: `icon ${theme === 'light' ? 'i-dark_mode' : 'i-light_mode'}`,
        'aria-hidden': 'true',
      }),
    );
    themeBtn.setAttribute(
      'aria-label',
      theme === 'light' ? '切换到深色主题' : '切换到浅色主题',
    );
  });

  const toggle = document.getElementById('sound-toggle');
  if (!toggle) return;
  const paint = () => {
    const on = isSoundEnabled();
    toggle.replaceChildren(
      h('span', { class: `icon ${on ? 'i-volume_up' : 'i-volume_off'}`, 'aria-hidden': 'true' }),
      document.createTextNode('声音'),
    );
    toggle.setAttribute('aria-pressed', String(on));
  };
  toggle.addEventListener('click', () => {
    setSoundEnabled(!isSoundEnabled());
    paint();
    if (isSoundEnabled()) blip('reply');
  });
  paint();
}
```

文件最末尾（原 `wireTopbar(); render();`）替换为：

```js
initTheme();
wireTopbar();
render();
```

- [ ] **Step 3.5: 跑测试确认绿**

Run: `npm run dom-check`
Expected: `✓ 主题切换: light → dark → light`，`全部通过 ✅`

- [ ] **Step 3.6: 请用户提交**

建议：`git add src/lib/theme.js index.html src/main.js scripts/dom-check.mjs && git commit -m "feat: 双主题状态机与顶栏切换入口（含回归断言）"`

---

### Task 4: styles.css 令牌化重写（两主题 + 背景 + 图标/头像/语义色阶）

**Files:**
- Modify: `src/styles.css`（整文件替换）

- [ ] **Step 4.1: 整文件替换 `src/styles.css`**

布局骨架、类名、动画与原文件一一对应；颜色全部改走令牌。新内容：

```css
/* 杠精陪练房 · 双主题（樱花粉浅色 / 宝蓝深色）——设计见 docs/draft/260910-样式优化设计方案.md */

:root,
:root[data-theme='light'] {
  /* 基础 chrome 令牌（规则 4：组件/按键/弹窗只用主题色系） */
  --accent: #e8a0bf;
  --accent-hover: #d98cb0;
  --accent-soft: #f8bbd0;
  --accent-strong: #c25c8d;
  --on-accent: #1f1f1f;
  --text: #1f1f1f;
  --muted: rgba(31, 31, 31, 0.62);
  --surface: rgba(255, 252, 253, 0.78);
  --surface-deep: rgba(255, 255, 255, 0.9);
  --line: rgba(31, 31, 31, 0.1);
  --canvas-bg: #fdf2f5;
  --bubble-ai: rgba(248, 187, 208, 0.4);
  --bubble-me: rgba(255, 255, 255, 0.9);
  --bg-full: url('../assets/bg-light.webp') center / cover no-repeat;
  /* 语义色阶（C 方案：冷静=主题色，上头/破防=暖红，脸涨红隐喻） */
  --stage-polite: #f8bbd0;
  --stage-sarcastic: #e8a0bf;
  --stage-agitated: #e0608f;
  --stage-breakdown: #c2185b;
  --hit-softspot: #c25c8d;
  --hit-self-destruct: #ad1457;
  --shadow: 0 24px 60px rgba(120, 60, 90, 0.18);
  --radius: 14px;
  --font: system-ui, -apple-system, 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', sans-serif;
}

:root[data-theme='dark'] {
  --accent: #283593;
  --accent-hover: #3a499e;
  --accent-soft: rgba(121, 134, 203, 0.28);
  --accent-strong: #9fa8da;
  --on-accent: #ffffff;
  --text: #ffffff;
  --muted: rgba(255, 255, 255, 0.64);
  --surface: rgba(15, 25, 62, 0.66);
  --surface-deep: rgba(10, 18, 48, 0.8);
  --line: rgba(255, 255, 255, 0.16);
  --canvas-bg: #0d1436;
  --bubble-ai: rgba(255, 255, 255, 0.08);
  --bubble-me: rgba(121, 134, 203, 0.28);
  --bg-full: url('../assets/bg-dark.webp') center / cover no-repeat;
  /* 语义色阶（C 方案：冷蓝 → 炽红） */
  --stage-polite: #9fa8da;
  --stage-sarcastic: #5c6bc0;
  --stage-agitated: #ff8a80;
  --stage-breakdown: #ff5252;
  --hit-softspot: #9fa8da;
  --hit-self-destruct: #ff8a80;
  --shadow: 0 24px 60px rgba(0, 0, 0, 0.5);
}

/* 不支持毛玻璃的环境：提高底色不透明度兜底可读性 */
@supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
  :root,
  :root[data-theme='light'] {
    --surface: rgba(255, 252, 253, 0.95);
    --surface-deep: #ffffff;
  }
  :root[data-theme='dark'] {
    --surface: rgba(13, 20, 52, 0.95);
    --surface-deep: #0d1436;
  }
}

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  height: 100%;
}

body {
  background: var(--bg-full);
  color: var(--text);
  font-family: var(--font);
  font-size: 16px;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  transition: color 0.2s;
}

*:focus-visible {
  outline: 2px solid var(--accent-strong);
  outline-offset: 2px;
}

#app {
  display: flex;
  flex-direction: column;
  height: 100%;
  max-width: 760px;
  margin: 0 auto;
}

/* ---------------- 图标（Material Symbols Outlined，自托管 SVG + mask） ---------------- */

.icon {
  display: inline-block;
  width: 1.25em;
  height: 1.25em;
  flex: 0 0 auto;
  vertical-align: -0.25em;
  background-color: currentColor;
  -webkit-mask: var(--icon) center / contain no-repeat;
  mask: var(--icon) center / contain no-repeat;
}

.star-dim {
  opacity: 0.3;
}

.i-settings { --icon: url('../assets/icons/settings.svg'); }
.i-volume_up { --icon: url('../assets/icons/volume_up.svg'); }
.i-volume_off { --icon: url('../assets/icons/volume_off.svg'); }
.i-light_mode { --icon: url('../assets/icons/light_mode.svg'); }
.i-dark_mode { --icon: url('../assets/icons/dark_mode.svg'); }
.i-close { --icon: url('../assets/icons/close.svg'); }
.i-star { --icon: url('../assets/icons/star.svg'); }
.i-target { --icon: url('../assets/icons/target.svg'); }
.i-bolt { --icon: url('../assets/icons/bolt.svg'); }
.i-keyboard { --icon: url('../assets/icons/keyboard.svg'); }
.i-theater_comedy { --icon: url('../assets/icons/theater_comedy.svg'); }
.i-cake { --icon: url('../assets/icons/cake.svg'); }

/* ---------------- 头像：主题色淡底圆 + 线性图标 ---------------- */

.avatar {
  display: inline-grid;
  place-items: center;
  width: 38px;
  height: 38px;
  border-radius: 50%;
  background: color-mix(in srgb, var(--accent) 22%, transparent);
}

.avatar .icon {
  width: 22px;
  height: 22px;
  color: var(--accent-strong);
}

.avatar.small {
  width: 30px;
  height: 30px;
}

.avatar.small .icon {
  width: 17px;
  height: 17px;
}

.avatar.inline {
  display: inline-flex;
  width: 20px;
  height: 20px;
  vertical-align: -4px;
}

.avatar.inline .icon {
  width: 14px;
  height: 14px;
}

/* ---------------- 顶栏 ---------------- */

.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 14px 18px;
  border-bottom: 1px solid var(--line);
  background: var(--surface);
  -webkit-backdrop-filter: blur(8px);
  backdrop-filter: blur(8px);
  transition: background-color 0.2s, border-color 0.2s;
}

.brand {
  font-weight: 700;
  letter-spacing: 0.02em;
}

.brand-en {
  margin-left: 8px;
  font-size: 12px;
  font-weight: 500;
  color: var(--muted);
  letter-spacing: 0.18em;
}

.topbar-right {
  display: flex;
  align-items: center;
  gap: 10px;
}

.badge {
  padding: 4px 10px;
  border: 1px solid var(--line);
  border-radius: 999px;
  font-size: 12px;
  color: var(--muted);
  white-space: nowrap;
}

.ghost-btn {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 5px 10px;
  border: 1px solid var(--line);
  border-radius: 999px;
  background: transparent;
  color: var(--muted);
  font-family: inherit;
  font-size: 12px;
  cursor: pointer;
  transition: color 0.15s, border-color 0.15s;
}

.ghost-btn:hover {
  color: var(--text);
  border-color: var(--accent-strong);
}

/* ---------------- 通用 ---------------- */

.screen {
  flex: 1;
  min-height: 0;
  display: flex;
  flex-direction: column;
}

.btn {
  padding: 10px 18px;
  border: 1px solid var(--line);
  border-radius: 10px;
  background: var(--surface-deep);
  color: var(--text);
  font-family: inherit;
  font-size: 15px;
  cursor: pointer;
  transition: transform 0.12s, border-color 0.15s, background-color 0.2s;
}

.btn:hover {
  border-color: var(--accent-strong);
}

.btn:active {
  transform: scale(0.98);
}

.btn.primary {
  background: var(--accent);
  border-color: var(--accent);
  color: var(--on-accent);
  font-weight: 700;
}

.btn.primary:hover {
  background: var(--accent-hover);
  border-color: var(--accent-hover);
}

.section-title {
  margin: 26px 0 12px;
  font-size: 14px;
  font-weight: 600;
  color: var(--muted);
  letter-spacing: 0.08em;
}

/* ---------------- 选人屏 ---------------- */

.select {
  flex: 1;
  overflow-y: auto;
  padding: 32px 18px 48px;
}

.hero-title {
  margin: 0 0 8px;
  font-size: 34px;
  line-height: 1.2;
  letter-spacing: -0.01em;
}

.hero-sub {
  margin: 0 0 28px;
  color: var(--muted);
  font-size: 15px;
}

.persona-grid {
  display: grid;
  gap: 14px;
}

.persona-card {
  display: block;
  width: 100%;
  padding: 18px;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: var(--surface);
  -webkit-backdrop-filter: blur(8px);
  backdrop-filter: blur(8px);
  color: inherit;
  font-family: inherit;
  text-align: left;
  cursor: pointer;
  transition: border-color 0.15s, transform 0.12s, background-color 0.2s;
}

.persona-card:hover {
  border-color: var(--accent-strong);
  background: var(--surface-deep);
  transform: translateY(-2px);
}

.persona-top {
  display: flex;
  align-items: center;
  gap: 12px;
}

.persona-name {
  font-weight: 700;
  font-size: 17px;
}

.persona-stars {
  display: inline-flex;
  align-items: center;
  gap: 2px;
  font-size: 12px;
  color: var(--accent-strong);
  letter-spacing: 0.08em;
}

.persona-tagline {
  margin: 12px 0 6px;
  color: var(--accent-strong);
  font-size: 15px;
}

.persona-tagline.small {
  margin: 2px 0 0;
  font-size: 12px;
  opacity: 0.85;
}

.persona-intro {
  margin: 0;
  color: var(--muted);
  font-size: 14px;
}

.footnote {
  margin: 26px 0 0;
  color: var(--muted);
  font-size: 13px;
  text-align: center;
}

.connect-hint {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: center;
  gap: 8px;
  margin-top: 22px;
  color: var(--muted);
  font-size: 13px;
}

.hint-btn {
  padding: 0;
  border: none;
  background: none;
  color: var(--accent-strong);
  font-family: inherit;
  font-size: 13px;
  text-decoration: underline;
  text-underline-offset: 3px;
  cursor: pointer;
}

.hint-btn:hover {
  opacity: 0.8;
}

/* ---------------- 设置弹窗 ---------------- */

.modal-backdrop {
  position: fixed;
  inset: 0;
  z-index: 50;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
  background: rgba(10, 10, 20, 0.55);
}

.modal {
  width: 100%;
  max-width: 480px;
  max-height: 100%;
  overflow-y: auto;
  padding: 22px;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: var(--surface);
  -webkit-backdrop-filter: blur(8px);
  backdrop-filter: blur(8px);
  box-shadow: var(--shadow);
}

.modal-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 18px;
}

.modal-title {
  margin: 0;
  font-size: 19px;
}

.modal-body {
  display: grid;
  gap: 14px;
}

.segmented {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1px;
  padding: 1px;
  border: 1px solid var(--line);
  border-radius: 10px;
  background: var(--line);
  overflow: hidden;
}

.seg-btn {
  padding: 9px 6px;
  border: none;
  background: var(--surface-deep);
  color: var(--muted);
  font-family: inherit;
  font-size: 13px;
  cursor: pointer;
  transition: background 0.15s, color 0.15s;
}

.seg-btn:hover {
  color: var(--text);
}

.seg-btn.is-active {
  background: var(--accent);
  color: var(--on-accent);
  font-weight: 700;
}

/* 弹窗里的「外观」两段控件（类名与 .seg-btn 区分开，别互相污染断言） */
.theme-seg {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1px;
  padding: 1px;
  border: 1px solid var(--line);
  border-radius: 10px;
  background: var(--line);
  overflow: hidden;
}

.theme-seg-btn {
  padding: 9px 6px;
  border: none;
  background: var(--surface-deep);
  color: var(--muted);
  font-family: inherit;
  font-size: 13px;
  cursor: pointer;
  transition: background 0.15s, color 0.15s;
}

.theme-seg-btn.is-active {
  background: var(--accent);
  color: var(--on-accent);
  font-weight: 700;
}

.field {
  display: grid;
  gap: 6px;
}

.field-label {
  font-size: 12px;
  color: var(--muted);
}

.field-hint {
  margin: 0;
  font-size: 13px;
  color: var(--muted);
}

/* 推理型模型会导致「聊几轮之后回不出话」，值得打断一下 */
.field-warn {
  margin: -4px 0 0;
  padding: 8px 10px;
  border-radius: 10px;
  background: color-mix(in srgb, var(--accent-strong) 10%, transparent);
  border: 1px solid color-mix(in srgb, var(--accent-strong) 30%, transparent);
  color: var(--accent-strong);
  font-size: 12px;
  line-height: 1.6;
}

.key-row {
  display: flex;
  gap: 8px;
}

.key-row .input {
  flex: 1;
  min-width: 0;
}

.key-row .ghost-btn {
  flex: 0 0 auto;
}

.modal .input {
  min-height: 0;
  padding: 9px 12px;
  font-size: 14px;
  font-family: inherit;
  color: var(--text);
  background: var(--surface-deep);
}

.modal select.input {
  cursor: pointer;
}

.modal-status {
  margin: 14px 0 0;
  min-height: 20px;
  font-size: 13px;
  color: var(--muted);
  word-break: break-word;
}

.modal-status.is-ok {
  color: var(--accent-strong);
}

.modal-status.is-error {
  color: var(--stage-breakdown);
}

.modal-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 18px;
}

.modal-actions .btn {
  flex: 1;
  min-width: 100px;
  padding: 10px 12px;
  font-size: 14px;
}

/* ---------------- 对线屏 ---------------- */

.duel {
  flex: 1;
  min-height: 0;
  display: flex;
  flex-direction: column;
  padding: 0 18px;
}

.duel-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 14px 0 10px;
}

.duel-who {
  display: flex;
  align-items: center;
  gap: 10px;
  min-width: 0;
}

.duel-meta {
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: 13px;
  color: var(--muted);
  white-space: nowrap;
}

.timer-label {
  font-variant-numeric: tabular-nums;
  color: var(--text);
}

.timer-label.urgent {
  color: var(--stage-breakdown);
  font-weight: 700;
}

/* 怒气条 */

.anger {
  padding-bottom: 12px;
}

.anger-head {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 7px;
  font-size: 12px;
  color: var(--muted);
}

.anger-num {
  font-size: 15px;
  font-weight: 700;
  color: var(--text);
  font-variant-numeric: tabular-nums;
}

.stage-pill {
  margin-left: auto;
  padding: 2px 10px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 700;
}

/* 阶段胶囊：实底阶段色 + 保证对比度的字色（浅色主题默认，深色见下方覆盖） */
:root .stage-pill.stage-polite { background: var(--stage-polite); color: #1f1f1f; }
:root .stage-pill.stage-sarcastic { background: var(--stage-sarcastic); color: #1f1f1f; }
:root .stage-pill.stage-agitated { background: var(--stage-agitated); color: #1f1f1f; }
:root .stage-pill.stage-breakdown { background: var(--stage-breakdown); color: #ffffff; }

:root[data-theme='dark'] .stage-pill.stage-polite { background: var(--stage-polite); color: #0d1436; }
:root[data-theme='dark'] .stage-pill.stage-sarcastic { background: var(--stage-sarcastic); color: #ffffff; }
:root[data-theme='dark'] .stage-pill.stage-agitated { background: var(--stage-agitated); color: #0d1436; }
:root[data-theme='dark'] .stage-pill.stage-breakdown { background: var(--stage-breakdown); color: #0d1436; }

.anger-track {
  position: relative;
  height: 10px;
  border-radius: 999px;
  background: color-mix(in srgb, var(--text) 12%, transparent);
  overflow: hidden;
}

.anger-fill {
  height: 100%;
  width: 0;
  border-radius: 999px;
  background: var(--stage-polite);
  transition: width 0.5s cubic-bezier(0.22, 1, 0.36, 1), background 0.4s;
}

/* 怒气填充色随阶段（颜色归 CSS，引擎只报 data-stage） */
.anger-fill[data-stage='polite'] { background: var(--stage-polite); }
.anger-fill[data-stage='sarcastic'] { background: var(--stage-sarcastic); }
.anger-fill[data-stage='agitated'] { background: var(--stage-agitated); }
.anger-fill[data-stage='breakdown'] { background: var(--stage-breakdown); }

.anger-tick {
  position: absolute;
  top: 0;
  width: 1px;
  height: 100%;
  background: color-mix(in srgb, var(--text) 40%, transparent);
}

/* 聊天记录 */

.log {
  flex: 1;
  min-height: 0;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 10px;
  padding: 14px 2px 18px;
  scroll-behavior: smooth;
}

.row {
  display: flex;
  align-items: flex-end;
  gap: 8px;
  max-width: 88%;
}

.row-ai {
  align-self: flex-start;
}

.row-me {
  align-self: flex-end;
  flex-direction: row-reverse;
}

.bubble {
  padding: 10px 14px;
  border-radius: 16px;
  font-size: 15px;
  word-break: break-word;
  white-space: pre-wrap;
}

.bubble-ai {
  background: var(--bubble-ai);
  border-bottom-left-radius: 5px;
}

.bubble-me {
  background: var(--bubble-me);
  border-bottom-right-radius: 5px;
}

.bubble.typing {
  display: flex;
  gap: 4px;
  padding: 14px;
}

.bubble.typing i {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--muted);
  animation: blink 1.2s infinite;
}

.bubble.typing i:nth-child(2) {
  animation-delay: 0.2s;
}

.bubble.typing i:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes blink {
  0%,
  60%,
  100% {
    opacity: 0.25;
  }
  30% {
    opacity: 1;
  }
}

.quip {
  align-self: center;
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 12px;
  color: var(--muted);
}

.quip-tag {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 1px 8px;
  border-radius: 999px;
  background: var(--surface-deep);
}

.quip-softspot .quip-tag {
  background: color-mix(in srgb, var(--hit-softspot) 14%, transparent);
  color: var(--hit-softspot);
}

.quip-self_destruct .quip-tag {
  background: color-mix(in srgb, var(--hit-self-destruct) 14%, transparent);
  color: var(--hit-self-destruct);
}

/* 远程 AI 掉线，这回合是本地模板说的 —— 必须看得出来 */
.fallback-line {
  align-self: center;
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 12px;
  color: var(--muted);
}

.fallback-tag {
  padding: 1px 8px;
  border-radius: 999px;
  background: color-mix(in srgb, var(--hit-self-destruct) 14%, transparent);
  color: var(--hit-self-destruct);
  white-space: nowrap;
}

.system-line {
  align-self: center;
  font-size: 12px;
  color: var(--muted);
  letter-spacing: 0.06em;
  padding: 4px 0;
}

/* 输入区 */

.composer {
  border-top: 1px solid var(--line);
  padding: 12px 0 18px;
}

.presets {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8px;
  margin-bottom: 10px;
}

.presets-label {
  font-size: 12px;
  color: var(--muted);
}

.preset-chip {
  padding: 5px 12px;
  border: 1px solid var(--line);
  border-radius: 999px;
  background: var(--surface);
  color: var(--text);
  font-family: inherit;
  font-size: 13px;
  cursor: pointer;
  transition: border-color 0.15s, color 0.15s, background-color 0.2s;
}

.preset-chip:hover:not(:disabled) {
  border-color: var(--accent-strong);
  color: var(--accent-strong);
}

.preset-chip.used,
.preset-chip:disabled {
  opacity: 0.32;
  cursor: not-allowed;
  text-decoration: line-through;
}

.input-row {
  display: flex;
  gap: 10px;
  align-items: flex-end;
}

.input {
  flex: 1;
  min-height: 54px;
  max-height: 120px;
  padding: 12px 14px;
  border: 1px solid var(--line);
  border-radius: 12px;
  background: var(--surface-deep);
  color: var(--text);
  font-family: inherit;
  font-size: 15px;
  line-height: 1.5;
  resize: none;
}

.input:focus {
  outline: none;
  border-color: var(--accent-strong);
}

.input:disabled {
  opacity: 0.55;
}

.send-btn {
  flex: 0 0 auto;
  height: 54px;
  padding: 0 22px;
  border: none;
  border-radius: 12px;
  background: var(--accent);
  color: var(--on-accent);
  font-family: inherit;
  font-size: 15px;
  font-weight: 700;
  cursor: pointer;
  transition: background-color 0.15s, transform 0.12s;
}

.send-btn:hover:not(:disabled) {
  background: var(--accent-hover);
}

.send-btn:active:not(:disabled) {
  transform: scale(0.98);
}

.send-btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

/* ---------------- 赛后报告 ---------------- */

.report {
  flex: 1;
  overflow-y: auto;
  padding: 32px 18px 56px;
}

.report-head {
  margin-bottom: 24px;
}

.result-badge {
  display: inline-block;
  padding: 6px 16px;
  border-radius: 999px;
  font-size: 14px;
  font-weight: 700;
}

.result-win {
  background: color-mix(in srgb, var(--hit-softspot) 14%, transparent);
  color: var(--hit-softspot);
}

.result-lose {
  background: color-mix(in srgb, var(--stage-breakdown) 14%, transparent);
  color: var(--stage-breakdown);
}

.result-draw {
  background: var(--surface-deep);
  color: var(--muted);
}

.result-sub {
  margin: 12px 0 0;
  color: var(--muted);
  font-size: 15px;
}

.title-card {
  display: flex;
  gap: 16px;
  align-items: flex-start;
  padding: 20px;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: var(--surface);
  -webkit-backdrop-filter: blur(8px);
  backdrop-filter: blur(8px);
}

.title-rank {
  flex: 0 0 auto;
  padding: 3px 9px;
  border-radius: 8px;
  background: var(--accent);
  color: var(--on-accent);
  font-size: 12px;
  font-weight: 800;
  letter-spacing: 0.06em;
}

.title-name {
  font-size: 22px;
  font-weight: 700;
  margin-bottom: 6px;
}

.title-desc {
  margin: 0;
  color: var(--muted);
  font-size: 14px;
}

.stats {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
  gap: 1px;
  margin-top: 22px;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  overflow: hidden;
  background: var(--line);
}

.stat {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 14px 16px;
  background: var(--surface);
  -webkit-backdrop-filter: blur(8px);
  backdrop-filter: blur(8px);
}

.stat-label {
  font-size: 12px;
  color: var(--muted);
}

.stat-value {
  font-size: 16px;
  font-weight: 600;
  display: inline-flex;
  align-items: center;
  gap: 6px;
}

.replay {
  margin: 0;
  padding: 0;
  list-style: none;
  display: grid;
  gap: 10px;
}

.replay-item {
  padding: 14px 16px;
  border: 1px solid var(--line);
  border-radius: 12px;
  background: var(--surface);
  -webkit-backdrop-filter: blur(8px);
  backdrop-filter: blur(8px);
  font-size: 14px;
}

.replay-me {
  color: var(--text);
}

.replay-ai {
  margin-top: 4px;
  color: var(--muted);
}

.replay-tag {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  margin-top: 8px;
  padding: 1px 9px;
  border-radius: 999px;
  background: var(--surface-deep);
  font-size: 12px;
  color: var(--muted);
}

.tag-softspot {
  background: color-mix(in srgb, var(--hit-softspot) 14%, transparent);
  color: var(--hit-softspot);
}

.tag-self_destruct {
  background: color-mix(in srgb, var(--hit-self-destruct) 14%, transparent);
  color: var(--hit-self-destruct);
}

.report-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 32px;
}

/* ---------------- 移动端 / 无障碍 ---------------- */

@media (max-width: 560px) {
  .hero-title {
    font-size: 27px;
  }

  .row {
    max-width: 94%;
  }

  .duel-meta {
    flex-direction: column;
    align-items: flex-end;
    gap: 2px;
  }

  .send-btn {
    padding: 0 16px;
  }
}

@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

注意：`.persona-avatar`、`.avatar-bubble` 的旧规则已删除（Task 6 起改用 `.avatar` 体系）；本任务只动 CSS，这两个旧类在 DOM 里还在，视觉暂由通用规则兜底——中间态可接受。

- [ ] **Step 4.2: 跑回归确认绿**

Run: `npm run dom-check`
Expected: `全部通过 ✅`（dom-check 不断言样式，此步防手滑改坏语法导致构建挂）

- [ ] **Step 4.3: 目验两主题**

Run: `npm run dev`，开 http://localhost:5173
Expected: 顶栏点月亮/太阳，背景图、半透明卡片、按钮、弹窗随主题即时切换；`.icon` 此时只有顶栏在用，其余 emoji 位置尚未替换（Task 6/7 处理）。

- [ ] **Step 4.4: 请用户提交**

建议：`git add src/styles.css && git commit -m "feat: styles.css 令牌化重写——樱花粉/宝蓝双主题 + 背景图 + 图标/头像体系"`

---

### Task 5: 设置弹窗——外观分段控件 + 关闭图标（先红后绿）

**Files:**
- Modify: `scripts/dom-check.mjs`（第 5 段内追加断言）
- Modify: `src/ui/settings-dialog.js`（imports、modal 结构、close）

- [ ] **Step 5.1: dom-check 第 5 段「清除」校验（`✓ 清除后徽章` 那行）之后、`$('.modal-head .ghost-btn').click()` 之前插入**

```js
// 外观：弹窗里也能切主题
const themeSeg = $$('.theme-seg-btn');
if (themeSeg.length !== 2) fail(`外观应该有 2 个选项，实际 ${themeSeg.length}`);
const themeBeforeSeg = document.documentElement.dataset.theme;
themeSeg[0].click();
if (document.documentElement.dataset.theme !== 'light') fail('点浅色应切到 light');
themeSeg[1].click();
if (document.documentElement.dataset.theme !== 'dark') fail('点深色应切到 dark');
themeSeg[themeBeforeSeg === 'dark' ? 1 : 0].click(); // 恢复原主题
console.log('✓ 设置弹窗外观切换正常');
```

- [ ] **Step 5.2: 跑测试确认红**

Run: `npm run dom-check`
Expected: FAIL `✗ 外观应该有 2 个选项，实际 0`

- [ ] **Step 5.3: 改 `src/ui/settings-dialog.js`**

文件头部 import 区（与现有 import 合并）加：

```js
import { applyTheme, onTheme } from '../lib/theme.js';
```

`backdrop` 构造中，在 `h('div', { class: 'modal-head' }, …)` 与 `body` 之间插入外观字段：

```js
      h('div', { class: 'field' },
        h('span', { class: 'field-label', text: '外观' }),
        h('div', { class: 'theme-seg' },
          h('button', { class: 'theme-seg-btn', type: 'button', text: '浅色', onclick: () => applyTheme('light') }),
          h('button', { class: 'theme-seg-btn', type: 'button', text: '深色', onclick: () => applyTheme('dark') }),
        ),
      ),
```

关闭按钮 `text: '✕'` 改为图标子节点：

```js
        h('button', {
          class: 'ghost-btn',
          type: 'button',
          'aria-label': '关闭',
          onclick: close,
        }, h('span', { class: 'icon i-close', 'aria-hidden': 'true' })),
```

`document.body.append(backdrop);` 之前加选中态同步订阅，`function close()` 里加退订：

```js
  const offTheme = onTheme((theme) => {
    backdrop.querySelectorAll('.theme-seg-btn').forEach((btn) => {
      btn.classList.toggle(
        'is-active',
        btn.textContent === (theme === 'light' ? '浅色' : '深色'),
      );
    });
  });
```

```js
  function close() {
    offTheme();
    document.removeEventListener('keydown', onKeydown);
    backdrop.remove();
    onResume?.();
  }
```

- [ ] **Step 5.4: 跑测试确认绿**

Run: `npm run dom-check`
Expected: `✓ 设置弹窗外观切换正常`，`全部通过 ✅`

- [ ] **Step 5.5: 请用户提交**

建议：`git add src/ui/settings-dialog.js scripts/dom-check.mjs && git commit -m "feat: 设置弹窗外观切换与图标化关闭按钮"`

---

### Task 6: 人设头像与难度星图标化

**Files:**
- Modify: `src/data/personas.js`（3 处 avatar 值）
- Modify: `src/main.js`（personaCard、viewDuel 的 duel-who、bubbleAI、typing 行、stat、viewReport 对手行）

- [ ] **Step 6.1: `src/data/personas.js` 三处替换**

`avatar: '⌨️'` → `avatar: 'keyboard'`；`avatar: '🍊'` → `avatar: 'theater_comedy'`；`avatar: '🍪'` → `avatar: 'cake'`

- [ ] **Step 6.2: `src/main.js` —— `personaCard()` 里两处**

头像行替换（原 `h('span', { class: 'persona-avatar', text: persona.avatar })`）：

```js
      h('span', { class: 'avatar' },
        h('span', { class: `icon i-${persona.avatar}`, 'aria-hidden': 'true' }),
      ),
```

星级行替换（原 `text: '★'.repeat(...)...`）：

```js
        h('div', { class: 'persona-stars', 'aria-label': `难度 ${persona.difficulty}/5` },
          ...Array.from({ length: persona.difficulty }, () =>
            h('span', { class: 'icon i-star', 'aria-hidden': 'true' })),
          ...Array.from({ length: 5 - persona.difficulty }, () =>
            h('span', { class: 'icon i-star star-dim', 'aria-hidden': 'true' })),
        ),
```

- [ ] **Step 6.3: `src/main.js` —— 对线屏头像两处**

`viewDuel()` 的 duel-who（原 `h('span', { class: 'persona-avatar small', text: persona.avatar })`）：

```js
        h('span', { class: 'avatar small' },
          h('span', { class: `icon i-${persona.avatar}`, 'aria-hidden': 'true' }),
        ),
```

`bubbleAI()`（原 `h('span', { class: 'avatar-bubble', text: duel.persona.avatar })`）：

```js
    h('span', { class: 'avatar small' },
      h('span', { class: `icon i-${duel.persona.avatar}`, 'aria-hidden': 'true' }),
    ),
```

`submitTurn()` 里的 typing 行（原 `h('span', { class: 'avatar-bubble', text: state.duel.persona.avatar })`）：

```js
    h('span', { class: 'avatar small' },
      h('span', { class: `icon i-${state.duel.persona.avatar}`, 'aria-hidden': 'true' }),
    ),
```

- [ ] **Step 6.4: `src/main.js` —— `stat()` 支持节点值 + 报告屏对手行**

`stat()` 整函数替换：

```js
function stat(label, value) {
  return h(
    'div',
    { class: 'stat' },
    h('span', { class: 'stat-label', text: label }),
    h('span', { class: 'stat-value' }, value),
  );
}
```

`viewReport()` 里对手统计行替换（原 `` stat('对手', `${duel.persona.avatar} ${duel.persona.name}`) ``）：

```js
    stat('对手', [
      h('span', { class: 'avatar inline', 'aria-hidden': 'true' },
        h('span', { class: `icon i-${duel.persona.avatar}` }),
      ),
      ` ${duel.persona.name}`,
    ]),
```

- [ ] **Step 6.5: 跑回归 + 目验**

Run: `npm run dom-check` → Expected: `全部通过 ✅`（`.stat-value` 的 textContent 拼接仍含人设名，断言兼容）
Run: `npm run dev` → Expected: 三屏头像全是主题色圆底线性图标，难度为五颗星（未点亮降透明度），报告屏「对手」行图标+名字。

- [ ] **Step 6.6: 请用户提交**

建议：`git add src/data/personas.js src/main.js && git commit -m "feat: 人设头像与难度星改为 Material Symbols 图标"`

---

### Task 7: 怒气条 data-stage 化 + 判定标签图标化 + 去 emoji 数据

**Files:**
- Modify: `src/lib/duel-engine.js`（STAGES 去 color）
- Modify: `src/main.js`（viewDuel 初始 fill、updateAngerUI、quipLine、viewReport replay-tag）
- Modify: `src/data/fallbacks.js`（HIT_QUIPS 去 emoji）

- [ ] **Step 7.1: `src/lib/duel-engine.js` STAGES 块替换**（颜色职责移交 CSS，数值权威不变）

```js
export const STAGES = [
  { id: 'polite', label: '礼貌', min: 0, max: 35 },
  { id: 'sarcastic', label: '阴阳', min: 35, max: 65 },
  { id: 'agitated', label: '上头', min: 65, max: 85 },
  { id: 'breakdown', label: '破防', min: 85, max: 100 },
];
```

- [ ] **Step 7.2: `src/main.js` `viewDuel()` 初始怒气条**

原 `refs.angerFill = h('div', { class: 'anger-fill', style: `width:${duel.anger}%` });` 替换：

```js
  refs.angerFill = h('div', {
    class: 'anger-fill',
    'data-stage': stage.id,
    style: `width:${duel.anger}%`,
  });
```

- [ ] **Step 7.3: `src/main.js` `updateAngerUI()` 最后一行**

原 `if (refs.angerFill) refs.angerFill.style.background = stage.color;` 替换：

```js
  if (refs.angerFill) refs.angerFill.dataset.stage = stage.id;
```

- [ ] **Step 7.4: `src/main.js` `quipLine()` 整函数替换**

```js
function quipLine(round) {
  const sign = round.delta > 0 ? '+' : '';
  const tagIcon =
    round.hitType === 'softspot'
      ? h('span', { class: 'icon i-target', 'aria-hidden': 'true' })
      : round.hitType === 'self_destruct'
        ? h('span', { class: 'icon i-bolt', 'aria-hidden': 'true' })
        : null;
  return h(
    'div',
    { class: `quip quip-${round.hitType}` },
    h('span', { class: 'quip-tag' }, tagIcon, HIT_LABELS[round.hitType] || '回合'),
    h('span', { class: 'quip-text', text: `${round.quip || ''} ${sign}${round.delta}` }),
  );
}
```

- [ ] **Step 7.5: `src/main.js` `viewReport()` 回放标签加图标**

原 `` h('div', { class: `replay-tag tag-${round.hitType}` }, `${HIT_LABELS[...]...}`) `` 替换：

```js
          h(
            'div',
            { class: `replay-tag tag-${round.hitType}` },
            round.hitType === 'softspot'
              ? h('span', { class: 'icon i-target', 'aria-hidden': 'true' })
              : round.hitType === 'self_destruct'
                ? h('span', { class: 'icon i-bolt', 'aria-hidden': 'true' })
                : null,
            `${HIT_LABELS[round.hitType] || '回合'} ${round.delta > 0 ? '+' : ''}${round.delta}`,
          ),
```

- [ ] **Step 7.6: `src/data/fallbacks.js` HIT_QUIPS 去 emoji**

`softspot: '🎯 扎心了'` → `softspot: '扎心了'`；`self_destruct: '💥 自爆'` → `self_destruct: '自爆'`

- [ ] **Step 7.7: 全量回归**

Run: `npm run smoke && npm run dom-check`
Expected: 双绿（smoke 不引用 STAGES.color，已核实；dom-check 断言的是 stage-pill 文本而非颜色）
另跑 `grep -rn "⌨️\|🍊\|🍪\|🎯\|💥\|★\|⚙️\|🔇\|🔊\|✕" src/ index.html` → Expected: 无输出（App 内 emoji 清零）

- [ ] **Step 7.8: 请用户提交**

建议：`git add src/lib/duel-engine.js src/main.js src/data/fallbacks.js && git commit -m "feat: 怒气条 data-stage 主题化与判定标签图标化，清零 App 内 emoji"`

---

### Task 8: 战报 Canvas 导出跟随主题

**Files:**
- Modify: `src/main.js`（exportCard）

- [ ] **Step 8.1: `exportCard()` 里 `ctx.scale(2, 2);` 之后插入令牌读取**

```js
  const css = getComputedStyle(document.documentElement);
  const token = (name) => css.getPropertyValue(name).trim();
  const C = {
    bg: token('--canvas-bg'),
    bar: token('--stage-breakdown'),
    muted: token('--muted'),
    text: token('--text'),
    card: token('--surface-deep'),
    rank: token('--accent-strong'),
  };
```

- [ ] **Step 8.2: 六处硬编码色替换**

`ctx.fillStyle = '#12100E';` → `ctx.fillStyle = C.bg;`
`ctx.fillStyle = '#FF4D4D';` → `ctx.fillStyle = C.bar;`
`ctx.fillStyle = '#8A8078';`（3 处）→ `ctx.fillStyle = C.muted;`
`ctx.fillStyle = '#EDEDED';`（2 处）→ `ctx.fillStyle = C.text;`
`ctx.fillStyle = '#26221E';` → `ctx.fillStyle = C.card;`
`ctx.fillStyle = '#FFD24A';` → `ctx.fillStyle = C.rank;`

- [ ] **Step 8.3: 跑回归 + 目验**

Run: `npm run dom-check` → Expected: `全部通过 ✅`
Run: `npm run dev` → 打完一局在浅色/深色各导出一次战绩图，检查底色/卡片/文字/顶部色条随主题。

- [ ] **Step 8.4: 请用户提交**

建议：`git add src/main.js && git commit -m "feat: 战绩图导出跟随当前主题（读取 CSS 令牌）"`

---

### Task 9: 文档同步 + 全量验证

**Files:**
- Modify: `docs/project/视听与界面.md`（令牌表与视觉描述更新为双主题现状）
- Modify: `docs/logs/260910.md`（追加开发日志）
- Modify: `README.md`（仅当其中提及旧「深夜/深色」视觉描述时）

- [ ] **Step 9.1: 跑 `grep -n "深夜\|深色\|12100E\|FFD24A" README.md docs/project/视听与界面.md`**

对 `docs/project/视听与界面.md`：重写「设计令牌」表为双主题两张表（浅色/深色，值与 Task 4 CSS 一致），「外壳」段补主题按钮与防闪色脚本描述，「关键组件」段的 stage-pill 四色改为「阶段色 = CSS 令牌，随主题」（≤120 行约束内）。README 若有视觉措辞则同步一句双主题说明，无则跳过。

- [ ] **Step 9.2: 写开发日志 `docs/logs/260910.md`**

当天文件不存在则创建，最新条目放最上方，格式 `## HH:MM ｜ 样式优化：双主题 + 背景图 + Material Symbols 图标体系`，四项：做了什么 / 动了哪些文件 / 关键决策及原因（引 spec 路径）/ 怎么验证的。

- [ ] **Step 9.3: 全量回归 + 手测清单**

Run: `npm run smoke && npm run dom-check && npm run build` → Expected: 全部成功
`npm run dev` 手测：两主题 × 三屏走查 ｜ 顶栏+弹窗双入口切换即时 ｜ 刷新后主题记忆 ｜ 清 localStorage 后首访跟随系统 ｜ bg-dark 四角文字可读性 ｜ 导出 PNG 双主题各一张 ｜ prefers-reduced-motion 动画关闭。

- [ ] **Step 9.4: 请用户提交（含 docs 与本计划/spec 的勾选状态）**

建议：`git add docs/ README.md && git commit -m "docs: 同步双主题视觉文档与开发日志"`
