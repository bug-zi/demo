> 状态：已执行完毕（260910），全部 6 任务完成。

# 皮肤系统实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans 本会话逐任务执行。步骤用 checkbox 跟踪。
> **Spec:** docs/draft/260910-皮肤系统设计方案.md（本计划唯一依据）
> **Git 约定：** 本项目所有 git 操作由用户手动完成，计划内不含任何 git 步骤；全部完成后向用户给出建议提交命令。

**Goal:** 在 GANG.AI 上加皮肤层：blossom（现状保留）+ midnight（深夜聊天窗，深色原版复刻 + 浅色白日暖米纸），顶栏弹层皮肤卡切换，皮肤与深浅正交解耦。

**Architecture:** `<html data-skin>` × `<html data-theme>` 双属性驱动 CSS 令牌四态；theme.js 扩展 skin 状态 API；新 skin-popover.js 弹层组件（挂 body 不走 render()）；皮肤元数据登记 src/data/skins.js。

**Tech Stack:** 原生 ESM + CSS 自定义属性 + jsdom 集成检查（dom-check）+ chrome-devtools 像素验证。

---

## 文件结构

| 文件 | 动作 | 职责 |
|---|---|---|
| `src/lib/theme.js` | 修改 | 扩展 skin 状态：getSkin/applySkin/setSkin/onSkin/initSkin |
| `src/data/skins.js` | 新建 | 皮肤登记表（弹层唯一数据源） |
| `src/styles.css` | 修改 | 末尾追加 midnight 四态令牌 + body/stage-pill 皮肤覆盖 + 弹层组件样式 |
| `public/assets/icons/palette.svg`、`check.svg` | 新建 | 新图标（npm pack 拷贝） |
| `index.html` | 修改 | 顶栏皮肤按钮 + 防闪脚本读 skin 键 |
| `src/ui/skin-popover.js` | 新建 | 皮肤选择弹层组件 |
| `src/main.js` | 修改 | initSkin 启动 + wireTopbar 接线皮肤按钮 |
| `scripts/dom-check.mjs` | 修改 | 第 7 节：皮肤状态 + 弹层 + 解耦断言 |
| `docs/**` | 修改 | Task 6 统一回写 |

---

### Task 1: 皮肤状态层（theme.js + skins.js，TDD 红→绿）

**Files:**
- Modify: `scripts/dom-check.mjs`（第 7 节状态断言，插在 `console.log('\n全部通过 ✅');` 之前）
- Modify: `src/lib/theme.js`（文件末尾追加 skin API）
- Create: `src/data/skins.js`

- [ ] **Step 1.1：写失败断言**（dom-check 第 6 节之后、最终通过日志之前插入）

```js
/* ------------------------------------------------------------------ */
/* 7. 皮肤：状态层 + 选择弹层 + 与深浅解耦                              */
/* ------------------------------------------------------------------ */

const { getSkin, setSkin } = await import('../src/lib/theme.js');
if (getSkin() !== 'blossom') fail(`默认皮肤应为 blossom，实际「${getSkin()}」`);
setSkin('midnight');
if (document.documentElement.dataset.skin !== 'midnight') fail('setSkin 应设置 data-skin');
if (window.localStorage.getItem('gang-ai:skin') !== 'midnight') fail('皮肤选择应写入 localStorage');
setSkin('blossom');
if (document.documentElement.dataset.skin !== 'blossom') fail('应能切回 blossom');
console.log('✓ 皮肤状态层：默认/切换/持久化');
```

- [ ] **Step 1.2：跑红**

Run: `npm run dom-check`
Expected: ✗ 失败（`getSkin` 未导出，TypeError: getSkin is not a function，退出码 1）

- [ ] **Step 1.3：实现 theme.js 皮肤 API**（文件末尾追加）

```js
/* ---------------- 皮肤（与深浅正交，见 docs/draft/260910-皮肤系统设计方案.md） ---------------- */

const SKIN_KEY = 'gang-ai:skin';
const skinListeners = new Set();

export function getSkin() {
  const saved = localStorage.getItem(SKIN_KEY);
  return saved === 'midnight' ? 'midnight' : 'blossom';
}

export function applySkin(skin) {
  if (skin !== 'blossom' && skin !== 'midnight') return;
  document.documentElement.dataset.skin = skin;
  for (const fn of skinListeners) fn(skin);
}

export function setSkin(skin) {
  if (skin !== 'blossom' && skin !== 'midnight') return;
  localStorage.setItem(SKIN_KEY, skin);
  applySkin(skin);
}

export function onSkin(fn) {
  skinListeners.add(fn);
  fn(getSkin());
  return () => skinListeners.delete(fn);
}

export function initSkin() {
  applySkin(getSkin());
}
```

- [ ] **Step 1.4：新建 src/data/skins.js**

```js
/** 皮肤登记表：皮肤选择弹层的唯一数据源，加皮肤只改这里。samples = [底色, 点缀色] 静态预览小样。 */
export const SKINS = [
  {
    id: 'blossom',
    name: '樱花与星夜',
    desc: '樱花粉与宝蓝星空，半透明卡片叠背景图',
    samples: { light: ['#fdf2f5', '#e8a0bf'], dark: ['#0d1436', '#9fa8da'] },
  },
  {
    id: 'midnight',
    name: '深夜聊天窗',
    desc: '炭黑暖棕与琥珀金，另配白日暖米纸',
    samples: { light: ['#f6f1e7', '#f0c243'], dark: ['#12100e', '#ffd24a'] },
  },
];
```

- [ ] **Step 1.5：跑绿**

Run: `npm run dom-check`
Expected: 第 1–7 节全绿，末行 `全部通过 ✅`

---

### Task 2: styles.css 四态令牌 + 皮肤级覆盖

**Files:**
- Modify: `src/styles.css`（**整体追加在文件末尾**——midnight 浅块 0-2-0 靠源顺序压过 blossom 深块；body 覆盖须在 dark body 规则之后；stage-pill 覆盖 0-4-0 盖过现有 0-3-0）

- [ ] **Step 2.1：文件末尾追加 midnight 令牌与覆盖块**

```css
/* ---------------- 皮肤：深夜聊天窗（midnight） ---------------- */
/* 块序即优先级：本节整体追加在文件末尾。midnight 浅块(0-2-0) 源顺序在后，
   压过前面的 blossom 深块(0-2-0)；midnight 深块(0-3-0) 最强。 */

:root[data-skin='midnight'] {
  /* 白日 · 暖米纸 */
  --accent: #f0c243;
  --accent-hover: #f5cd66;
  --accent-soft: rgba(143, 106, 0, 0.14);
  --accent-strong: #8f6a00;
  --on-accent: #241f14;
  --text: #241f14;
  --muted: #857b6e;
  --surface: #fffcf5;
  --surface-deep: #f0e9da;
  --line: #e2d9c6;
  --canvas-bg: #f6f1e7;
  --bubble-ai: #f0e9da;
  --bubble-me: #e3eddf;
  /* 语义色阶：原版绿→琥珀→橙→红的浅底加深版 */
  --stage-polite: #5f7d5f;
  --stage-sarcastic: #8f6a00;
  --stage-agitated: #c96f24;
  --stage-breakdown: #d33a3a;
  --hit-softspot: #8f6a00;
  --hit-self-destruct: #d33a3a;
  --shadow: 0 24px 60px rgba(120, 90, 30, 0.18);
}

:root[data-skin='midnight'][data-theme='dark'] {
  /* 深夜（原版 1:1） */
  --accent: #ffd24a;
  --accent-hover: #ffdc70;
  --accent-soft: rgba(255, 210, 74, 0.16);
  --accent-strong: #ffd24a;
  --on-accent: #241f14;
  --text: #ededed;
  --muted: #8a8078;
  --surface: #1a1714;
  --surface-deep: #221e1a;
  --line: #2e2924;
  --canvas-bg: #12100e;
  --bubble-ai: #26221e;
  --bubble-me: #2e3a2e;
  --stage-polite: #8fa88f;
  --stage-sarcastic: #ffd24a;
  --stage-agitated: #ff9a4d;
  --stage-breakdown: #ff4d4d;
  --hit-softspot: #ffd24a;
  --hit-self-destruct: #ff4d4d;
  --shadow: 0 24px 60px rgba(0, 0, 0, 0.55);
}

/* midnight 不用背景图：压掉 blossom 的两张图（须在 dark body 规则之后） */
:root[data-skin='midnight'] body {
  background-image: none;
}

/* stage-pill 回原版「素底彩字」（0-4-0 盖过前面 0-3-0 的实底药丸） */
:root[data-skin='midnight'] .stage-pill.stage-polite { background: var(--surface-deep); color: var(--stage-polite); }
:root[data-skin='midnight'] .stage-pill.stage-sarcastic { background: var(--surface-deep); color: var(--stage-sarcastic); }
:root[data-skin='midnight'] .stage-pill.stage-agitated { background: var(--surface-deep); color: var(--stage-agitated); }
:root[data-skin='midnight'] .stage-pill.stage-breakdown { background: var(--surface-deep); color: var(--stage-breakdown); }
```

- [ ] **Step 2.2：dev 浏览器验四态令牌与背景图关闭**

前置：`npm run dev`（后台，记下端口 P）。chrome-devtools 开 `http://localhost:P`，依次执行并核对：

```js
() => {
  const de = document.documentElement;
  const read = () => {
    const cs = getComputedStyle(document.body);
    const css = getComputedStyle(de);
    return { canvas: css.getPropertyValue('--canvas-bg').trim(), accent: css.getPropertyValue('--accent').trim(), img: cs.backgroundImage.slice(0, 60) };
  };
  const out = {};
  de.dataset.skin = 'blossom'; de.dataset.theme = 'light'; out.bl = read();
  de.dataset.theme = 'dark'; out.bd = read();
  de.dataset.skin = 'midnight'; de.dataset.theme = 'light'; out.ml = read();
  de.dataset.theme = 'dark'; out.md = read();
  de.dataset.skin = 'blossom'; de.dataset.theme = 'light';
  return out;
}
```

Expected: `bl.canvas=#fdf2f5 / bd.canvas=#0d1436`（img 为 bg-light/bg-dark webp）；`ml.canvas=#f6f1e7 / md.canvas=#12100e` 且两者 `img: none`；`ml.accent=#f0c243 / md.accent=#ffd24a`。

- [ ] **Step 2.3：跑 dom-check 确认无回归**

Run: `npm run dom-check`
Expected: `全部通过 ✅`

---

### Task 3: palette/check 图标 + index.html 皮肤按钮与防闪

**Files:**
- Create: `public/assets/icons/palette.svg`、`public/assets/icons/check.svg`
- Modify: `src/styles.css`（`.i-settings` 规则后追加两条）、`index.html`

- [ ] **Step 3.1：npm pack 拷贝两个图标**

```bash
cd "$TEMP" && npm pack @material-symbols/svg-400@0.47.2 && tar -xzf material-symbols-svg-400-0.47.2.tgz && cp package/files/palette.svg package/files/check.svg "D:/Code/黑客松/260909未央黑客松/public/assets/icons/"
```

Expected: 无报错，两个文件就位（与现有 12 个同源同版本）。

- [ ] **Step 3.2：styles.css 加图标类**（`.i-settings` 规则之后）

```css
.i-palette {
  -webkit-mask-image: url('/assets/icons/palette.svg');
  mask-image: url('/assets/icons/palette.svg');
}

.i-check {
  -webkit-mask-image: url('/assets/icons/check.svg');
  mask-image: url('/assets/icons/check.svg');
}
```

- [ ] **Step 3.3：index.html 顶栏加按钮**（引擎徽章与主题按钮之间）

```html
          <button id="skin-btn" class="ghost-btn" type="button" aria-label="皮肤选择" aria-haspopup="dialog">
            <span class="icon i-palette" aria-hidden="true"></span>
          </button>
```

- [ ] **Step 3.4：index.html 防闪脚本加 skin 键**（内联脚本设完 dataset.theme 之后）

```js
        var sk = localStorage.getItem('gang-ai:skin');
        if (sk === 'midnight') document.documentElement.dataset.skin = sk;
```

- [ ] **Step 3.5：dev 验证图标真实加载**（content-type 验法，200≠对）

```bash
curl -s -o /dev/null -w "%{http_code} %{content_type}\n" http://localhost:P/assets/icons/palette.svg
curl -s -o /dev/null -w "%{http_code} %{content_type}\n" http://localhost:P/assets/icons/check.svg
```

Expected: 两行均 `200 image/svg+xml`

---

### Task 4: skin-popover.js 组件 + main.js 接线（TDD 红→绿）

**Files:**
- Modify: `scripts/dom-check.mjs`（第 7 节状态断言之后追加 UI 断言）
- Create: `src/ui/skin-popover.js`
- Modify: `src/main.js`、`src/styles.css`（组件样式）

- [ ] **Step 4.1：追加失败断言**（第 7 节 `console.log('✓ 皮肤状态层…')` 之后）

```js
const skinBtn = $('#skin-btn');
if (!skinBtn) fail('顶栏没有皮肤按钮');
skinBtn.click();
const skinPop = $('.skin-popover');
if (!skinPop) fail('点皮肤按钮没弹层');
const skinCards = $$('.skin-card');
if (skinCards.length !== 2) fail(`皮肤卡应有 2 张，实际 ${skinCards.length}`);
const midnightCard = skinCards.find((c) => c.dataset.skinId === 'midnight');
if (!midnightCard) fail('没有午夜皮肤卡');
midnightCard.click();
if (document.documentElement.dataset.skin !== 'midnight') fail('点卡应切到 midnight');
if (!$('.skin-popover')) fail('点卡后弹层应保持打开（便于对比切换）');
if ($('.skin-card.is-active').dataset.skinId !== 'midnight') fail('active 卡应随切换更新');

// 解耦：midnight 下主题按钮仍可切深浅
const themeBefore2 = document.documentElement.dataset.theme;
$('#theme-toggle').click();
if (document.documentElement.dataset.theme === themeBefore2) fail('midnight 下主题按钮应仍可切换');
$('#theme-toggle').click();

// 切回 blossom 并用 Esc 关闭
skinCards.find((c) => c.dataset.skinId === 'blossom').click();
if (document.documentElement.dataset.skin !== 'blossom') fail('应切回 blossom');
document.dispatchEvent(new window.KeyboardEvent('keydown', { key: 'Escape' }));
if ($('.skin-popover')) fail('Esc 应关闭皮肤弹层');
console.log('✓ 皮肤弹层：两卡切换/解耦/高亮更新/Esc 关闭');
```

- [ ] **Step 4.2：跑红**

Run: `npm run dom-check`
Expected: ✗ `顶栏没有皮肤按钮`（Task 3 已加按钮则走到 `点皮肤按钮没弹层`，均算红）

- [ ] **Step 4.3：新建 src/ui/skin-popover.js**

```js
/**
 * 皮肤选择弹层：顶栏 palette 按钮触发，挂 body（不走 render()，
 * 对线中随时打开，不暂停计时——轻量即时选择）。
 * 点卡即切皮肤（与深浅解耦），弹层保持打开便于对比；Esc / 点外部关闭。
 */
import { h } from '../lib/dom.js';
import { onSkin, setSkin } from '../lib/theme.js';
import { SKINS } from '../data/skins.js';

let root = null;
let offSkin = null;
let btn = null;

function onDocMouseDown(e) {
  if (root && !root.contains(e.target) && !(btn && btn.contains(e.target))) closeSkinPopover();
}

function onDocKeyDown(e) {
  if (e.key === 'Escape') closeSkinPopover();
}

function syncActive() {
  if (!root) return;
  const current = document.documentElement.dataset.skin || 'blossom';
  for (const card of root.querySelectorAll('.skin-card')) {
    card.classList.toggle('is-active', card.dataset.skinId === current);
  }
}

function sample([bg, accent], label) {
  return h('span', { class: 'skin-sample' },
    h('span', { class: 'skin-sample-bg', style: `background:${bg}` }),
    h('span', { class: 'skin-sample-dot', style: `background:${accent}` }),
    h('span', { class: 'skin-sample-label' }, label));
}

function skinCard(skin) {
  return h('button', {
    type: 'button',
    class: 'skin-card',
    'data-skin-id': skin.id,
    onclick: () => setSkin(skin.id),
  },
    h('span', { class: 'skin-card-head' },
      h('span', { class: 'skin-card-name' }, skin.name),
      h('span', { class: 'skin-card-check' },
        h('span', { class: 'icon i-check', 'aria-hidden': 'true' }))),
    h('span', { class: 'skin-card-desc' }, skin.desc),
    h('span', { class: 'skin-card-samples' },
      sample(skin.samples.light, '浅色'),
      sample(skin.samples.dark, '深色')));
}

export function openSkinPopover(trigger) {
  if (root) return;
  btn = trigger;
  root = h('div', { class: 'skin-popover', role: 'dialog', 'aria-label': '皮肤选择' },
    h('div', { class: 'skin-pop-title' }, '皮肤'),
    h('div', { class: 'skin-cards' }));
  for (const skin of SKINS) root.querySelector('.skin-cards').append(skinCard(skin));
  document.body.append(root);
  offSkin = onSkin(syncActive);
  syncActive();
  document.addEventListener('mousedown', onDocMouseDown);
  document.addEventListener('keydown', onDocKeyDown);
}

export function closeSkinPopover() {
  if (!root) return;
  root.remove();
  root = null;
  btn = null;
  if (offSkin) {
    offSkin();
    offSkin = null;
  }
  document.removeEventListener('mousedown', onDocMouseDown);
  document.removeEventListener('keydown', onDocKeyDown);
}

export function toggleSkinPopover(trigger) {
  if (root) closeSkinPopover();
  else openSkinPopover(trigger);
}
```

- [ ] **Step 4.4：main.js 三处接线**

① import 区加（与 settings-dialog 导入并列）：
```js
import { toggleSkinPopover } from './ui/skin-popover.js';
```
② theme 导入扩为：
```js
import { initTheme, initSkin, toggleTheme, onTheme } from './lib/theme.js';
```
③ `wireTopbar()` 内 settings-btn 监听之后加：
```js
  const skinBtn = document.getElementById('skin-btn');
  skinBtn?.addEventListener('click', () => toggleSkinPopover(skinBtn));
```
④ 启动序列改为：
```js
initTheme();
initSkin();
wireTopbar();
render();
```

- [ ] **Step 4.5：styles.css 文件末尾追加弹层组件样式**

```css
/* ---------------- 皮肤选择弹层 ---------------- */

.skin-popover {
  position: fixed;
  top: 60px;
  right: 16px;
  z-index: 60;
  width: 300px;
  padding: 14px;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: var(--surface);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  box-shadow: var(--shadow);
}

.skin-pop-title {
  margin-bottom: 10px;
  font-size: 13px;
  font-weight: 700;
  color: var(--muted);
  letter-spacing: 0.08em;
}

.skin-cards {
  display: grid;
  gap: 8px;
}

.skin-card {
  display: grid;
  gap: 6px;
  padding: 12px;
  border: 1px solid var(--line);
  border-radius: 12px;
  background: var(--surface-deep);
  color: var(--text);
  font-family: inherit;
  font-size: 13px;
  text-align: left;
  cursor: pointer;
  transition: border-color 0.15s, transform 0.12s;
}

.skin-card:hover {
  border-color: var(--accent-strong);
  transform: translateY(-1px);
}

.skin-card.is-active {
  border-color: var(--accent);
}

.skin-card-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}

.skin-card-name {
  font-size: 14px;
  font-weight: 700;
}

.skin-card-check {
  display: none;
  color: var(--accent-strong);
}

.skin-card.is-active .skin-card-check {
  display: inline-flex;
}

.skin-card-desc {
  color: var(--muted);
  font-size: 12px;
}

.skin-card-samples {
  display: flex;
  gap: 8px;
}

.skin-sample {
  display: inline-flex;
  flex: 1;
  align-items: center;
  gap: 5px;
  padding: 4px 6px;
  border: 1px solid var(--line);
  border-radius: 8px;
  font-size: 11px;
  color: var(--muted);
}

.skin-sample-bg {
  width: 22px;
  height: 14px;
  border: 1px solid var(--line);
  border-radius: 4px;
}

.skin-sample-dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
}

@media (max-width: 560px) {
  .skin-popover {
    left: 12px;
    right: 12px;
    width: auto;
  }
}
```

- [ ] **Step 4.6：跑绿 + 全量回归**

Run: `npm run dom-check && npm run smoke`
Expected: dom-check 第 1–7 节全绿（含新 UI 断言）；smoke 9 ✓

---

### Task 5: 构建产物 + 4 态像素验证

- [ ] **Step 5.1：build 检查**

Run: `npm run build`
Expected: 0 条 "didn't resolve" 警告。随后：

```bash
ls dist/assets/icons | wc -l            # 14
grep -o 'ffd24a' dist/assets/*.css | wc -l   # ≥2（深夜 accent 至少出现两处）
grep -q 'f6f1e7' dist/assets/*.css && echo midnight-light-ok
```

- [ ] **Step 5.2：dev 浏览器 4 态截图 + 像素采样**（chrome-devtools）

对 (blossom,light) / (blossom,dark) / (midnight,light) / (midnight,dark) 四态各执行：
1. evaluate 设 `dataset.skin/theme`，读 `getComputedStyle(document.body)` 的 backgroundColor/backgroundImage；
2. 截屏存盘，PIL 采样页面边缘与主按钮区域像素。

Expected（表）：

| 态 | body 背景计算值 | 边缘像素 |
|---|---|---|
| blossom/light | rgb(253,242,245) + bg-light.webp | 粉调 |
| blossom/dark | rgb(13,20,54) + bg-dark.webp | 宝蓝调 |
| midnight/light | rgb(246,241,231)，image none | 暖米 |
| midnight/dark | rgb(18,16,14)，image none | 炭黑 |

- [ ] **Step 5.3：持久化 + 防闪验证**

dev 页执行 `localStorage.setItem('gang-ai:skin','midnight'); location.reload();` → 重载后 `document.documentElement.dataset.skin === 'midnight'` 且首帧无闪（脚本先于渲染执行）。

- [ ] **Step 5.4：弹层真机走查**

浏览器点 palette 按钮：弹层出现、点午夜卡全站换肤、active ✓ 跟随、Esc 关闭、点外部关闭、主题按钮在 midnight 下照常工作。

---

### Task 6: 文档回写

- [ ] **Step 6.1：`docs/project/视听与界面.md`** — 「主题机制」节升级为「主题与皮肤机制」（data-skin×data-theme 正交、localStorage 双键、弹层组件）；令牌表改为 blossom/midnight × 浅/深 四列（引用 spec §2 值表）；图标清单 12→14（palette/check）；关键组件加皮肤弹层一条。
- [ ] **Step 6.2：`CLAUDE.md`** — theme.js 条目补 skin API 与 `gang-ai:skin` 键；styles.css 条目补「双皮肤四态令牌，midnight 实色无背景图」；Architecture 列表补 `src/ui/skin-popover.js` 与 `src/data/skins.js` 一行。
- [ ] **Step 6.3：`docs/工作台.md`** — 新功能区本轮条目移入归档区顶部「第2轮（260910）｜皮肤系统」，附完成说明。
- [ ] **Step 6.4：`docs/logs/260910.md`** — 顶部追加 `## HH:MM ｜ 皮肤系统：双皮肤四态 + 顶栏弹层切换`（四项：做了什么/动了哪些文件/关键决策及原因/怎么验证的，用当时本地时间）。
- [ ] **Step 6.5：归档 spec/plan** — `docs/draft/260910-皮肤系统设计方案.md`、`260910-皮肤系统实施计划.md` 移入 `docs/design/样式改版/archive/`，各加状态行「已落地（260910）」；`docs/design/样式改版/design.md` 顶部状态行补一句第 2 轮已落地。
- [ ] **Step 6.6：给用户建议提交命令**（不代执行）：

```bash
git add -A && git commit -m "feat: 皮肤系统——樱花与星夜/深夜聊天窗双皮肤四态，顶栏弹层切换"
```

---

## Self-Review

- **Spec 覆盖：** §0 四决策→T4 行为/T2 CSS/T1/T3；§1–2 令牌→T2 全值；§3 覆盖→T2；§4 API+登记表→T1；§5 弹层→T4；§6 图标→T3；§7 防闪→T3.4；§8 测试→T1/T4/T5；§9 文档→T6；§10 风险→块序注释+T5 像素兜底、外点排除按钮（4.3 onDocMouseDown）、jsdom 不依赖 matchMedia（skin 逻辑无）。✓
- **占位符扫描：** 无 TBD/TODO；docs 任务给出精确内容要点。✓
- **类型一致性：** `toggleSkinPopover(trigger)` 4.3 定义 = 4.4 调用；`dataset.skinId` = `data-skin-id`；`i-check/i-palette` 全文一致。✓
