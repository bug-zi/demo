# 260913

## 00:26 ｜ 对线房拆锁：全开放 + 推荐路线（闯关梯降级为引导）

- **做了什么**：拆除选人屏的线性闯关门（原第 i 关需通 i−1，锁卡只露「???」），18 个人设全部直接可打；梯子降级为纯引导——每房第一个未通关的关高亮「推荐」（徽标文案由「当前关」改「推荐」），已通关「复刷 · 奖励30%」不变；经济层零改动（首通/复刷 0.3 档/通关账照旧）
- **动了哪些文件**：`src/lib/ladder.js`（删 `isLevelUnlocked`，新增 `nextRecommended()` 纯函数）、`src/main.js`（import、`levelStateOf` 推荐语义、`personaCard`/`personaAvatarEl` 删 locked 分支）、`src/styles.css`（删两处 `.persona-card.locked` 组与孤儿 `.i-lock` 类）、`scripts/profile-check.mjs`（删解锁断言，新增空洞推荐/全通无推荐断言）、`scripts/dom-check.mjs`（§2/2b/2c/2d/19 全节改写为全开放+推荐语义，§19b 改为跳关直进断言）、CLAUDE.md、README.md、docs/project/{00-总览,成长系统}.md
- **关键决策及原因**：
  - 全开放而非双模式/试玩券：成就墙+称号+复刷奖励已承担目标感，心池已承担失败成本，锁是双重门禁且把 demo 广度藏在一排「???」后面（与开发者 brainstorm 共识）
  - 推荐逻辑下沉 `ladder.js` 纯函数：全开放后通关账会出现空洞（如通 0、3 关跳过 1、2），旧 `maxCleared+1` 推法在空洞下指错；DOM 侧不好测空洞（`state.profile` 以内存为单一事实，注 localStorage 会被设置弹窗读写覆盖），纯函数层用 profile-check 直测
  - `lockedPersonaCard()` 保留：它是 `category.locked` 未开放分组的占位卡（注册表机制），与关卡锁无关
- **怎么验证的**：smoke / profile-check / dom-check 三项全绿（dom-check 含改写后的推荐落位、跳关直进、复刷、进度保留断言）；playwright 真浏览器过完整链路——18 卡真名可点、跳关直进「五彩斑斓甲方」成功、打完圣人后「推荐」正确移到亲戚且圣人卡出现「复刷 · 奖励30%」（截图 `output/playwright/select-open-recommend.png`）
- **遗留**：`public/assets/icons/lock.svg` 因删锁失去使用者，文件留在原处未删（untracked，由开发者决定去留）

## 00:08 ｜ 对局退出改回对线房 + 活局恢复条 + 返回组件全局美化

- **做了什么**：
  1. 对局屏退出按钮由「← 大厅」改为「← 对线房」（新增 `exitToSelect()`，进出路径对称）；离场不毁局机制原样复用（计时/AI 屏外记账/暂停态零改动）
  2. 对线房新增 `.resume-strip` 活局恢复条：对方头像 + 名字 +「第 N 轮 · 进行中」（与大厅角标同口径 `currentRound()`）+「回到对局」一键续局（复用 `enterDuelModule()`）；无活局不渲染
  3. 胜负在对线房背后落账时同步 `render()` 刷掉过期恢复条（此前只有大厅角标有此待遇）
  4. 返回组件全局美化：新增自托管 `arrow_back.svg` 图标 + `.i-arrow-back` mask 类；`dom.js` 新增 `backBtn(label, onclick, extraClass)` 统一构造器；`.back-btn` 改图标+文字 flex 胶囊（surface 底、hover 加深 + accent 描边、active 按压），10 处调用点（select/duel/资料库/成就墙/擂台×6）全部换用
  5. 换人静默替换活局（沿用 `startDuel()` 现状，不加确认）
- **动了哪些文件**：`src/lib/dom.js`（+backBtn）、`src/main.js`（exitToSelect/resumeStrip/两处按钮/离场落账刷 select）、`src/ui/arena.js`、`src/ui/library.js`、`src/ui/wall.js`（按钮换 backBtn）、`src/styles.css`（.i-arrow-back/.back-btn 重写/.resume-strip 组）、`public/assets/icons/arrow_back.svg`（新增）、`scripts/dom-check.mjs`（流 4/6/9/9b/13 断言改对线房落点 + 新增 9c 替换小节）、CLAUDE.md（导航描述、dom.js 职责）
- **关键决策及原因**：
  - 活局保留 + 恢复条而非退出即弃局：与既有「中途离场不毁局」原则一致，且误触退出不再有代价（开发者确认）
  - 换人不弹确认：开新局是显式动作，恢复条已让活局可见，静默替换够用（开发者确认）
  - 落账刷 select：恢复条和大厅角标同为「离场快照」，胜负落账后不刷新会残留假活局——dom-check 流 9 当场抓出此问题
- **怎么验证的**：`npm run dom-check` 全绿（含新增 9c：恢复条断言 + 静默替换 + 新局无旧记录）；smoke/library-check/arena-check/profile-check 全绿；浏览器实测双皮肤 × 双主题返回按钮/恢复条观感
- **设计文档**：`docs/superpowers/specs/2026-09-12-duel-exit-navigation-design.md`
