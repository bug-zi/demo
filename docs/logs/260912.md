# 260912 开发日志

## 22:45 ｜ 美术轨量产：gpt-image-2 管线打通 + A1 全量 18 立绘接入 + A2/A3/A4 小样落地（TDD 红→绿 + 实机验收）

- **做了什么**：美术升级轨从「等人工出图」转为「API 量产 + 代码接入」。①**管线打通**：中转站（token.aiedulab.cn）实查 `/models` 定名 `gpt-image-2`，补 `ART_IMAGE_MODEL` 后 generate.mjs 全量跑通——A1 立绘 15 张补齐（累计 18/18）、A2 背景 gang/lobby 两张、A3 边框+插画、A4 徽章三枚（bronze/gold/king）。②**A1 全量接入**：18 人设全部写 `portrait` 字段，头像统一走 `personaAvatarEl()`（选人卡/对局头/AI 气泡/输入中/贴纸行/战报对手栏六处）；分享卡头像升级为立绘圆形裁切（drawPortrait），缺失回退 Path2D 图标重绘，再回退首字圆；img 加 onerror 换回线性图标（文件 404 也不出破图）。③**A2 背景接入**：大厅/对局/战报屏根插 `screen-bg` 固定背景层（叠在皮肤背景之上 + `--surface` 主题色纱遮罩，四态令牌自适配），`ROOM_BG` 表驱动，缺图自然无层。④**A3 接入**：胜局战报插 `spotlight` 破防插画（居中 340px 限宽，onload 亮出/onerror 摘除）；分享卡胜局叠 `frame/win.png` 边框（加载失败原样导出）。⑤**A4 接入**：成就卡头部渲染徽章图位（onload 亮出/404 摘除），锁定卡随卡片降透明。⑥TDD 全程：dom-check 先后三次红→绿（六处 portrait 断言、A2/A3/A4 四处接入断言、A1 全量后 §2d 语义重写）。⑦新工具：`resize-portraits.ps1`（批量缩方图）、`finish-art.ps1`（cover 裁切/JPG/圆形蒙版/白底抠透/中心镂空）。
- **动了哪些文件**：`src/data/personas.js`（18×portrait）、`src/main.js`（personaAvatarEl+screenBg+spotlightEl+三屏根挂层）、`src/styles.css`（avatar-img/screen-bg/spotlight，EOF 追加纯令牌）、`src/ui/wall.js` + `wall.css`（badgeImg+样式）、`src/lib/share-card.js`（drawPortrait/drawFrame+降级链）；`scripts/art/generate.mjs`（background 参数透传）、新增 `manifests/a3-samples.json`、`manifests/a4-samples.json`、`resize-portraits.ps1`、`finish-art.ps1`、`kill-mcp-chrome.ps1`（工具脚本）；`scripts/dom-check.mjs`（§1/§2c/§2d/§20 断言增改）；`public/assets/art/`（portraits×18、bg×2、frame×1、spotlight×1、badges×3，共 25 文件 ≈9MB）；`.env.art`（补 MODEL，gitignored）；a2-backgrounds/a1-portraits manifest 尺寸改 1536×1024。
- **关键决策及原因**：①**中转站 503 定位**——带 `background:'transparent'` 的请求必 503（账号池不支持该参数），不带则全部成功；透明资产改「白底生成 + KeyWhite 抠透 / 圆形蒙版」确定性后处理，不再依赖 API 参数。②破防插画 PNG 1084KB 压不进 300KB 预算，改存 JPG（91KB，无透明需求，圆角由 CSS 裁），spec 表的 `.png` 命名按预算条款让步。③成就徽章曾全隐——`loading="lazy"` + `display:none` 是死组合（无布局盒永不进交集，onload 永不触发），去掉 lazy；教训：dom-check 的「节点存在」断言验不出这种「加载永不发生」问题，必须实机看图。④gpt-image 系不支持 1440×900，背景按 1536×1024 生成、入库 cover 裁切。⑤spotlight 首版 100% 宽吞掉整屏，限宽 340px 居中。⑥立绘 PNG 单张 374–563KB 超 spec 单文件 300KB 预算（总量 ~9MB 在 20MB 总预算内），样张阶段保留，定稿批统一 8-bit 量化或转 JPG。
- **怎么验证的**：`npm run dom-check` 全绿（§2d 全量立绘断言、锁定关锁图标、对局头部立绘、大厅/对局背景层、战报插画位、墙徽章图位）；smoke / profile-check 全绿无回归；实机 Chrome：选人三卡立绘混排协调、对局双皮肤头像自然、gang 背景暖纸模式纱下透景、破防插画尺寸合宜、成就墙三徽章（解锁亮/锁定灰）正确渲染、分享卡立绘头像+无边框降级导出正常；素材文件级目检（边框镂空干净、徽章蒙版对位精准）。截图存 `out/shots/`。
- **遗留给开发者验收**：四类小样全部上线等你过目——大厅/对局背景、战报插画、胜局边框（打一局胜局导出战绩图可见）、成就墙徽章；背景纱浓度（62%）与插画限宽（340px）两个参数随时可调。

## 20:16 ｜ 轨C 准备：A1 立绘 / A2 背景 manifest 定稿（提示词清单可直接手动生成）

- **做了什么**：轨C 正式批次的前置——写定 `scripts/art/manifests/a1-portraits.json`（18 条人设立绘 prompt，name=personaId，1024×1024，杠精房冷灰蓝 / 谈判房商务暖灰 / 情商房暖杏治愈的分档底色，全部沿用风格小样的统一前缀+无文字约束+负面尾缀）与 `scripts/art/manifests/a2-backgrounds.json`（lobby/gang/deal/eq 四张 1440×900 空场景背景，无人物、四角留白）。prompt 与 `generate.mjs` 的 manifest 驱动格式完全对齐：开发者可选择任一生图工具手动逐条生成，或待 `.env.art` 通道可用后 `node scripts/art/generate.mjs a1-portraits` 批量跑。
- **动了哪些文件**：`scripts/art/manifests/a1-portraits.json`、`scripts/art/manifests/a2-backgrounds.json`（新增）。
- **关键决策及原因**：①去水印白名单查证结果——公开文档无自助开关，属工单申请制（合规书面说明用途），开发者决定**先不做**：水印只在角落，定风格与 demo 运行零阻塞，转由开发者用任意生图工具手动生成，prompt 清单即本次交付物；②wangyou/naicha 两条 prompt 直接复用风格小样原文（拍板基线不漂移）；③bar/工牌/传单/手机屏等道具一律写明「纯色空白」防生成器画上文字——风格指南「画面内绝对无文字」对道具同样适用；④情商房底色用暖杏/暮蓝紫细分，区别于样张的暖米黄，避免六张底色雷同。
- **怎么验证的**：manifest 数组结构对齐 generate.mjs 的读取逻辑（`[{name, prompt, size}]`，name 合法且与 personas.js id 一一对应）；A1 18 条 = 三房各 6 与 M6 后名册一致。生成效果待开发者手动出图后人工筛选（风格拍板门在量产前，两小样 wangyou/naicha 为现成基线）。

## 19:24 ｜ M6 打磨收官：数值平衡 pass + 全量回归 + 文档回写 + 3.0.0（轨 A 全部完成）

- **做了什么**：3.0 轨 A 最后一个里程碑 M6 落地，成长系统正式收官。①**数值平衡推演**（一次性脚本 `out/balance-sim.mjs`，gitignored）：全程 18 胜零败 = 1120 XP → **Lv.6**，升级坡度前紧后松（前期 2–3 胜一级 → 后期 5–9 胜一级）；理论收入上限 = 首通 2060 + 升级币 500 + 成就币 3950（15 青铜/12 白银/7 金/2 王）= 6510，唯一硬消费买心 50 且心池封顶 5——消费端天然有界；复刷币恒低于买心价（d5 复刷 45 < 50，防刷成立），复刷经验照发约 9 场一级；心经济无死锁（连败 5 局才触底，一场 d1 首通 70 币就够买一颗）。**结论：ECONOMY 表零调整**。②**全量回归**：smoke / profile-check / dom-check / library-check / arena-check 五套件全绿。③**文档回写 + 版本号**：package.json 2.0.0 → **3.0.0**；成长系统 spec 与总方案按 design/README 归档流程移入 `docs/design/3.0成长系统/archive/`（两文件顶部加已归档状态行）；要点回写 `docs/project/`——新建 `成长系统.md`（档案 schema/ECONOMY 表/结算幂等/闯关梯/阻力参数/成就墙称号/M6 平衡结论），重写 `人设系统.md`（5→18 人设三房关卡表、字段结构补 finale/guard/drift/portrait 留位、d1 教学关合计 100 与灭火局 84 的双向节奏、新增检查单对齐三道闸门），`对线规则.md` 补 guard/drift 结算段与 driftPaidRounds/roundSeconds 状态、情商房「装修中」改已开放、ROUND_SECONDS 标注应用默认不限时；`00-总览.md` 全面更新（v3.0.0、六屏→七屏、名片卡、目录职责补 profile/ladder/achievements/wall、profile-check 命令）；根 README 更新（三房 6 关名册、闯关成长玩法表、目录树补 3.0 文件）；CLAUDE.md 更新（概览、七屏、名片卡、关卡梯、guard/drift、成长层架构条目、settleDuel 数据流、profile-check）；`design/README.md` 模块登记更新（成长系统已落地、人设扩充 B1/B2 ✅）。④**顺手清理**：`main.js` 名片卡成就计数的「成就 · 即将开放」死分支删除（成就表现有 36 条，恒走计数分支）——全库最后一个「即将开放」UI 语义退场；`好友擂台.md` 一句过时的「情商房占位」说明更正；`ladder.js`/`profile.js` 头注释与人设扩充 spec 的旧 spec 路径全部指向 archive 新路径。
- **动了哪些文件**：`package.json`（3.0.0）、`src/main.js`（死分支清理）、`src/lib/ladder.js` / `src/lib/profile.js`（注释路径）；`docs/design/3.0成长系统/`（两文件移入 `archive/` + 状态行）、`docs/design/README.md`、`docs/design/3.0人设扩充/designs-specs.md`（依据路径）、`docs/project/成长系统.md`（新）、`docs/project/人设系统.md`（重写）、`docs/project/对线规则.md`、`docs/project/00-总览.md`、`docs/project/好友擂台.md`（一句）、`README.md`、`CLAUDE.md`；新增 `out/balance-sim.mjs`（gitignored 推演脚本，可复跑）。
- **关键决策及原因**：①M6 的平衡 pass 选择「推演先行、数据说话」——五项健康指标（升级坡度/收入上限/防刷阈值/心经济死锁点/新手 15 分钟线）全部落在合理区间，**不为调而调**，ECONOMY 表原样收官；B3 若加人设，首通币与关卡自动按表增长，无需回头调参。②归档严格按 design/README 既有判定执行（日终日志 + spec 自带「三阶段全落地后归档」条款），但**人设扩充 spec 留守**（B3 未设计仍在用）、**美术升级 spec 留守**（轨 C 等用户开水印/定旗舰模型）；归档后按流程全库检索旧路径并更新活动文档引用（logs 历史不改）。③`好友擂台.md` 那句「即将开放只剩情商房」是 260911 落地时点的事实快照，本次按现状文档的定位更正为当前事实；`新功能.md` 归档区的同类句子是历史记录，按约定不改。④main.js 死分支清理属 M6 打磨范畴：数据缺席降级路径在成就表这里没有真实场景（表不会为空），留着只会误导后来人以为存在「无成就表」的运行态。
- **怎么验证的**：`out/balance-sim.mjs` 六节推演输出（全程曲线/逐级胜数/成就币分账/通胀合计/复刷与心经济/新手体验线）；五套件全绿（M6-T2 全量回归）；`main.js` 清理后 `npm run dom-check` 重跑全绿（名片卡/成就墙/toast 各节无回归）；`profile-check` 复跑全绿 + `node -e` 确认 version=3.0.0；grep 复查旧 spec 路径、「装修中」「即将开放」在 src/ 与 docs/project/ 活动文档中清零（剩余命中全在 archive/idea 历史文档与 dom-check 断言字符串里，属预期）。
- **遗留给开发者手测**（与 B1/B2 手测项合并）：演示模式开关→打一场败局确认心不掉；成就墙四类来源各真触发一条；断网完整爬梯体验。

## 18:47 ｜ B2 落地：杠精房 +4 / 谈判房 +3（每房满 6）+ 「互异软肋」新闸门（TDD 红→绿 + 实机验证）

- **做了什么**：3.0 轨 B 第二批次收官，三房各满 6 关。①新增 7 人设——杠精房：shengren d1 评论区圣人（理性讨论双标）/ banping d4 半瓶水大神（行话唬人）/ qungui d4 群规执法者（双标执法）/ yimoulun d5 阴谋论大师（自洽证据链）；谈判房：chefan d1 二手车贩子（原版原漆）/ zhongjie d3 房产中介（焦虑话术）/ hr d4 HR 谈薪官（压薪薪酬包）；插入位置按 spec §3 关序（同星按数组序：banping→qungui、laoban→hr），每房形成 1→2→3→4→4→5 梯；阻力参数按星档（d3 0.95/2、d4 0.9/2、d5 0.85/3）；finale 七人全部配齐（read-none/recall/quit/lights-off/rapid 复用）。②smoke「预设命中三互异软肋」新闸门：旧关键词自检只验「命中即算过」，挡不住预设 A 误串软肋 B 的键（本轮实测抓到两处）；smoke 总人数 11→18、profile-check 两房梯序断言翻转。③dom-check 全面适配：总卡 18、初始解锁 3；新增 §2c 新手关节（评论区圣人三预设恰好破百→亲戚解锁露真名→通关账 [0]）；**选卡全部从下标改为按人设名定位**（pickCard 助手，B2/B3 插关不再平移断言）；§19c 改打贩子、解锁断言改砍价摊主；§19e 通关账 [0,1,2]；no-hearts 口径 18。④七枚自托管图标（self_improvement/science/gavel/travel_explore/directions_car/apartment/badge）+ mask 类。
- **动了哪些文件**：`src/data/personas.js`（+7 人设）、`src/styles.css`（7 个 mask 块）、`public/assets/icons/`（7 枚 SVG）；`scripts/smoke.mjs`（人数 + 互异软肋闸门）、`scripts/profile-check.mjs`（梯序/levelIndexOf 翻转）、`scripts/dom-check.mjs`（pickCard 化 + §2/§2b/§2c/§19/口径适配）；`docs/design/3.0人设扩充/designs-specs.md`（B2 状态回写）。
- **关键决策及原因**：①**两房 d1 教学关软肋合计定为 100**（圣人 34/32/34、贩子 34/32/34）——初稿 30/26/28=84 是照情商房「100−和」的节奏写的，点火局要 ≥100 才判胜，导致三预设差 16 打不死（dom-check §2c 抓住）；三预设恰好破百 = 新手关三手过，与情商房 d1 三手消气同节奏；②**「串键」盲区升级成闸门**——贩子预设「过户记录」误串 report 键的「记录」（34+17 递减=51 而非 66）、banping「截图水印」误串 evidence 键的「截图」，旧自检只见 softspot 就放行；现要求每条预设命中互异键，纯 Node 复算（matchSoftspot 逐条）先行验证再入库；③dom-check 选卡从「数组下标」全面转向「按人设名定位」——插关平移下标的坑 B2 已经踩了两轮，B3 起只加数据不动测试骨架；④§19a 的 DOM 梯序断言只验「首关真名 + 锁卡 ???」——锁卡本来就只露 ???，全序正本归 profile-check 的 ladderFor 断言。
- **怎么验证的**：`npm run smoke` 全绿（18 人设逐字段过检 + 关键词自检 18×3 全 ✓ + 互异软肋闸门 + fire 十二人 finale kind 覆盖五档）；`npm run profile-check` 全绿（两房梯序 贩子→摊主→中介→老板→HR→甲方 / 圣人→亲戚→网友→半瓶水→群规→阴谋论）；`npm run dom-check` 23 节全绿（新增 §2c 新手关全链路）；library/arena 无回归；Chrome 实机：三房梯（圣人/贩子 1 星首关 + 5 锁，星预告 2/3/4/4/5）→ 七图标全 200 → 实打圣人局（怒气 34→66→100、rapid 破防演出、d1 结算 +40/+70、三成就行）→ 深夜皮肤下圣人「复刷」徽章 + 亲戚「当前关」高亮无破相；控制台干净。

## 18:20 ｜ B1 情商房落地：6 灭火人设 + eq 房解锁 + 人设数据完整性闸门（TDD 红→绿 + 实机像素验证）

- **做了什么**：3.0 轨 B 第一批次收官。①新增情商房 6 人设（naicha d1 奶茶同事 / chadui d2 被插队路人 / gezi d3 被放鸽子好友 / laodie d4 催婚老爹 / zhigyou d5 深夜赶稿挚友 / chaping d5 一星差评买家），全部按 spec §2 灭火写作规则：opener=怒气开局、breakdown=6–8 条暴怒常规池（85–100 段即玩家最先听到的状态）、stages 三键按消气阶梯反向、softspotReactions 三钥匙齐全、presets 逐条命中三心结、finale 复用五种演出做消气向文案（撤回狠话/气消走人/连环台阶/挂电话/下线赶稿）、guard/drift 按星档（d3 0.95/2、d4 0.9/2、d5 0.85/3）；②`categories.js` 删 eq `locked`——情商房正式解锁，三房全开；③smoke 增「人设数据完整性」节（spec §4.1 一次性闸门）：必填字段/软肋×3（delta 20–40 域、keywords≥6 且禁单字）/stages 三键/ reactions 三键齐/ breakdown 分房口径（fire≥1、eq 6–8）/id 唯一/阻力参数域/finale 合法性；④smoke 适配：总人数断言 5→11、finale kind 互不重复约束改为按 fire 房圈定（B1 起 eq 复用 kind 合法）；profile-check §14 情商房梯子断言翻转 + §15 eq_clear/all_clear 补达标用例（空房守卫代码保留）；dom-check 适配：总卡数 5→11、初始解锁 2→3、eq 占位断言改为 1 解锁+5 锁关卡梯、no-hearts 口径 5→11、增 21 节情商房完整对局（怒气 100 开局→三心结哄到 16 判胜→消气战报→通关账落盘→演示模式回落）；⑤六枚自托管 Material Symbols 图标（local_cafe/groups/schedule_send/elderly/nightlife/star_half）+ 对应 `.i-*` mask 类。
- **动了哪些文件**：`src/data/personas.js`（+6 人设 ≈300 行）、`src/data/categories.js`（eq 解锁）、`src/styles.css`（6 个 mask 块）、`public/assets/icons/`（6 枚新 SVG）；`scripts/smoke.mjs`（§8 完整性节 + §5/§5.6 适配）、`scripts/profile-check.mjs`（§14/§15 翻转与补例）、`scripts/dom-check.mjs`（§2/§2b/§18e/§19/收尾口径 + 新 §21）；`docs/design/3.0人设扩充/designs-specs.md`（B1 状态回写）。
- **关键决策及原因**：①**自己的完整性闸门抓住两处真问题**——zhigyou 关键词「歇」是单字（spec 明令禁止，包含匹配会大面积误命中，改为「歇会儿」）；zhigyou 预设「第三章我来顶…明早我交」因「我顶」「明早交」非连续子串而未命中软肋（smoke 既有「关键词自检」行兜住，改写为「这部分我写…明早交」）；②naicha 平衡教学关刻意不配 guard/drift（d1=缺省），三心结 30+28+26 恰好三手到 16，与 M2 难度坡度表一致；③smoke 的「关键词自检」（每条 preset 必须命中自家软肋）从 A1 期就在，B1 六人全部借它验了预设设计——这条隐性约束写进了写作流程；④dom-check §21 收尾的 no-hearts 断言必须在选人屏上做（大厅没有 persona-card，第一次跑挂在 0≠11）；⑤皮肤为纯令牌驱动，eq 新界面零写死色，深夜皮肤实机抽查结构完好。
- **怎么验证的**：`npm run smoke` 全绿（新增：11 人设逐字段过检（情商房 6 人）+ 关键词自检 11×3 全 ✓ + fire 五人 finale kind 不重复）；`npm run profile-check` 全绿（情商房梯子 naicha→chaping、eq_clear/all_clear 正反例、空房守卫仍在）；`npm run dom-check` 22 节全绿（新 §21：怒气 100 起步 / 开场白=「我的奶茶！！」/ log data-stage=breakdown / 三 chip 到 16 判胜 / 「TA 消气了」+读心术大师 / 心 无变动 / cleared.eq=[0] 落盘）；library/arena 无回归；Chrome 实机：eq 六关梯（首关 current、五星预告与星档一致、i-local_cafe 渲染）→ 实打 naicha 一局（怒气 100→70→42→16，消气阶梯 agitated→sarcastic 演染正确）→ 战报「TA 消气了」+ d1 结算 +40/+70 + 三成就行 + 心结命中 3/3 统计 → 深夜皮肤（暖米纸）下选人屏无破相；控制台干净。

## 17:44 ｜ M4 成就墙收官：36 条成就 + 称号入册/佩戴四处同步 + 墙视图 + 场外 toast 通道（TDD 红→绿 + 实机像素验证）

- **做了什么**：3.0 轨 A 第三阶段 M4 全程落地。①成就数据层：`achievements.js` 填充 36 条（六类 × 6，`group` 字段供墙分区），新增 5 个 stats 字段（selfDestructWins/fullRoundWins/winsWithTitle/comebackWins/demoWins）+ grantBattle 增 `ev.selfDestructs`/`ev.rounds` 记账（触底反弹在写 lastOutcomes 前判定）；②称号双源：`titles.js` 增 `ACHIEVEMENT_TITLES`（甲方收割机）+ `ALL_TITLES` 全图 + `titleById()`，`settleDuel()` 在 grantBattle 扫描**之前**把 `pickTitle()` 结果入册（去重），称号数成就同局生效；③成就墙 UI：`src/ui/wall.js` + `wall.css`（library/arena 同款自包含模块），名片头/称号柜（9 格图鉴、点击佩戴/再点摘下）/成就网格（六类分区、解锁亮+时间、隐藏未解锁 ???）；main.js 挂 `wall` 屏，大厅名片卡点击改进墙（进墙前扫一次成就）；④toast 通道：右下角堆叠 `.ach-toast`（3s 自消），对局成就只进战报结算区不进队列，队列只收场外解锁（进墙扫描/资料库切场景/擂台终盘）；⑤挂勾：`library.js` 增可选 `onSceneViewed`（真实场景 id 才记账，旧调用方不传无感），`arena.js` 增 `onArenaEnd`（paintRecap 恰好上报一次，paintIntro 复位；win 口径 = 分出胜负）；⑥佩戴称号四处同步：大厅名片卡、对局 meta 行、战报结算区称号行、分享卡 PNG（`exportDuelCard` 增可选 `wornTitle`，标题卡与统计区之间画「称号 · X」）。
- **动了哪些文件**：`src/data/achievements.js`（36 条全表）、`src/data/titles.js`（三表 + ALL_TITLES + titleById）、`src/lib/profile.js`（defaultStats/sanitize 增 5 字段、grantBattle 记账、引 MAX_ROUNDS）、`src/ui/wall.js` + `src/ui/wall.css`（新增）、`index.html`（wall.css `<link>`）、`src/main.js`（settleDuel 入册、toast 通道、scanAchievements/enterWall/wallView、render wall 分支、名片卡改造成就计数+佩戴名+进墙、library/arena 回调注入、meta 行与结算区称号行、exportCard 传 wornTitle）、`src/ui/library.js` / `src/ui/arena.js`（可选回调）、`src/lib/share-card.js`（wornTitle 行）、`src/styles.css`（EOF 追加 .ach-toast/.profile-title/.duel-title-tag，纯令牌）；`scripts/profile-check.mjs`（§15 成就表逐条正反例 + §15b 记账 + §6/§7/§10 金币断言适配成就币）、`scripts/dom-check.mjs`（增 20 节四小节：进墙结构/佩戴同步/toast 通道/亮格）、`scripts/arena-check.mjs`（onArenaEnd 恰好一次 + 再来一局不复报）；`docs/design/3.0成长系统/designs-specs.md` §M4 四处实现批注。
- **关键决策及原因**：①「名满天下」从 12 个称号改为**集齐全部称号**——称号全图实为 9 枚（8 赛后 + 1 成就专属），12 永远不可达成，清单按总方案「可增删」条款修正，阈值由 ALL_TITLES.length 驱动、B1 后自动对齐；②房通关类成就经 ladderFor 派梯且**空房不算通关**——情商房 B1 前梯子为空，否则 `cleared.eq.length >= 0` 恒真会开局送金成就；③买心与「氪金玩家」存在天然联动（首买当场解锁回补 50 币），profile-check 用「预持成就」隔离原用例、另加一条正向联动断言；④jsdom 教训 × 3：监听器异常不冒泡到 click() 调用方（wallView 曾返回视图对象而非节点，静默白屏——用最小 repro + 插桩定位）；墙/库点击后整柜重绘 + 大厅往返重挂视图，**断言前一律现查 DOM**；⑤收藏/笔记动作本身不扫描，解锁落在下一次扫描点（进墙/切场景/对局/买心/擂台），dom-check 20 节据此设计成「攒满 10 收藏 → 切场景触发扫描弹剪报家」的确定性链路；⑥arena 的 win 口径：hot-seat 无主客，按「分出胜负」记账（平局不计），spec 已批注。
- **怎么验证的**：`npm run profile-check` 16 节全绿（36 条逐条达标/不达标构造 + applyAchievements 幂等 + 成就称号入册 + grantBattle 五新字段记账/豁免 + 血脉压制/一鼓作气/向死而生随结算发放；arena-check 新用例实证有牙：临时禁用 reportEnd → 红，恢复 → 绿）；`npm run dom-check` 22 节全绿（20 节：名片卡直达/称号柜 9 格/成就 36 格/佩戴摘下与名片卡同步/切场景触发扫描解锁/3s 自消/已领不重复弹/亮格）；smoke / library-check（可选参数无感）/ arena-check 全绿无回归；Chrome 实机：新档名片卡成就 0/36 → 墙结构（樱花皮肤令牌无破相、wall.css 200）→ 实打首胜（结算 +90 币 + 三成就行 + 称号「未佩戴」行）→ 称号柜佩戴 → 名片卡/名片头/对局 meta 行三处同步 → 复刷局（+27 复刷币、结算称号行、**盛装出席**成就当场触发）→ 分享卡 PNG 像素采样：称号行区域 4963 个琥珀像素、上方空白带 0（称号行落位准确）→ 资料库收藏+切场景真弹「成就解锁 · 淘到好货 · +50 金币」右下角 toast；控制台无报错（仅历史 form-field a11y 提示）。

## 16:23 ｜ M2 房内闯关梯 UI 落地：选人屏关卡化 + 复刷/解锁全链路（TDD 红→绿 + 实机验证）

- **做了什么**：M2 收官。选人屏从「自由选人」改为「房内关卡梯」——房间内人设按难度升序排关：未解锁关只露「???」+ 星数预告（锁图标头像、不可点）；第一个已解锁未通关的卡高亮描边 + 「当前关」角标；已通关卡带「复刷 · 奖励30%」标识、随时可进。对局携带 `duel.ladder = { roomId, levelIndex }`，终局结算按它写通关账并以入账前的 `isCleared` 判复刷（复刷金币 = 首通×0.3，经验照发）。新增 lock.svg（Material Symbols 自托管）。dom-check 增 19 节：锁关展示/不可进/通关解锁下一关/复刷可进/进度落盘保留/演示模式回落，并把 8 处选卡点、分组卡数断言、18c 结算金额全部适配到梯子语义。
- **动了哪些文件**：`src/main.js`（import ladder 四函数；categoryGroup 改 ladderFor 渲染 + levelStateOf 派生；personaCard 加关卡状态五态；startDuel 挂 ladder 坐标；settleDuel 传 roomId/levelIndex/isReplay）；`src/styles.css`（EOF 追加 .persona-card.current 高亮 / .level-badge 两款 / 锁头像弱化色，纯令牌）+ `.i-lock` mask；新增 `public/assets/icons/lock.svg`；`scripts/dom-check.mjs`（增 19 节 + 既有 8 处选卡索引/卡数/复刷币档断言适配）；`docs/logs/260912.md`。
- **关键决策及原因**：①复刷判定放在 grantBattle **之前**调 isCleared——通关账写入后恒真，时机错了复刷档就失效；②锁定卡与心=0 禁用正交：locked 恒 disabled，no-hearts 按快照追加，两种锁互不覆盖（18e 的「全卡禁用」断言因此保持 5 张口径）；③dom-check 选卡从「固定下标」逐步走向「按房间分组内取」——梯子改变了全局卡序（亲戚升为杠精首关），旧下标会静默打到别人（15 节的 hit+贴纸数学因此错位，实为 13 节选到了网友）；④19d 弃局改打完：活局主卡=续局语义挡住「退大厅再进选人」，改用自爆两发收场走报告（演示模式不扣心）。
- **怎么验证的**：`npm run dom-check` 21 节全绿（新增：锁定卡 ???+星数预告+disabled / 点锁不进对线 / 通关谈判首关后画饼老板露真名解锁 / cleared 卡带复刷标识且可进 / localStorage 通关账 [0,1]+[0] 落盘且重渲染不丢）；profile-check / smoke(27✓) / library-check / arena-check 全绿无回归；Chrome 实机：清档 → 选人屏梯子结构正确（亲戚当前关、网友 ???+3星+锁头像，lock.svg 200）→ 实打亲戚一关 → 报告 → 亲戚变复刷徽章、网友解锁变当前关高亮、cleared 落盘。

## 15:19 ｜ M2 闯关梯逻辑层落地：ladder.js + 引擎 guard/drift + 通关账（TDD 红→绿）

- **做了什么**：3.0 轨 A 的 M2 里程碑逻辑层全程 TDD 落地（UI 梯子下一步）。新增 `src/lib/ladder.js` 四个纯函数（`ladderFor` 房内人设按 difficulty 升序派梯、`levelIndexOf`、`isLevelUnlocked` 首关天然解锁、`isCleared`），关卡数据不落盘、加人设自动长梯；`duel-engine.js` 接入两个阻力参数——guard（玩家主动输出软肋/命中/贴纸四舍五入折算，自爆/沉默豁免，extinguish 先翻方向再乘）与 drift（环境漂移，每回合结算前补付新完成间隔，胜局推送永不被吃）；`profile.js` grantBattle 胜局写 `cleared` 通关账（去重、败局不写、陌生房间不写）；现有 5 人设按星补参（网友 d3 0.95/2、老板 d4 0.9/2、甲方 d5 0.85/3；d2 两人 = 缺省 1.0/0 免字段）。美术轨并行：智谱 cogview-3-flash 管线实跑成功，两张风格小样出图（场景向可用、人物向不宜，水印待控制台关闭）。
- **动了哪些文件**：新增 `src/lib/ladder.js`；`src/lib/duel-engine.js`（createDuel 加 driftPaidRounds 记账、recordTurn 插 drift 补付与 guard 折算两段）；`src/lib/profile.js`（grantBattle 通关账写入）；`src/data/personas.js`（3 人设补 guard/drift）；`scripts/smoke.mjs` 增 5.7 节五小节（guard 乘算/豁免、drift 双向+封顶+增量语义、不吞胜局、默认参数回归）；`scripts/profile-check.mjs` 增 14 节闯关梯 + 反转 M1「不写通关账」断言；`scripts/dom-check.mjs` 16c/18c 脚本局改为「1 chip + 4 打字逐手等落地」新节奏；`docs/design/3.0成长系统/designs-specs.md` §M2.2 drift 公式实现修正批注；`out/art-raw/style-samples/` 两张小样（gitignore 目录）。
- **关键决策及原因**：①**修正了自己 spec 里的 drift 公式错误**——初稿「每回合按 `drift×elapsed` 重付既往全部漂移」是平方叠加，8 局累计 −56，d3+ 人设数学上不可战胜；正确语义是每回合只补一个新间隔（8 局累计 −drift×7，d3 约 5 手可破百，难度坡度符合立项意图），引擎按增量补付实现并在 elapsed=2 处加了探针断言，spec 已按仓库约定回写批注；②smoke 第 6 节贴纸机制的测试载体从网友换摊主——网友 retrofit 后带 guard 会污染精确增量断言，机制测试与阻力参数解耦；③dom-check 两处脚本局被 retrofit 打破（老 4 手节奏在 guard/drift 下打不满 100），改写时揪出两个测试侧真坑：**预设 chip 是一次性牌**（复点无效）与 **chip 回合未落地就打字会被 `refs.input.value=''` 清掉**——新节奏「逐手等 input.disabled 解锁」把这两类时序问题钉死；④dom-check 16c 最后一击不等待（轮询窗口会把 rapid 演出整个等完，收场行漏拍）。
- **怎么验证的**：`npm run smoke` 27 条 ✓（新增 guard：软肋 30→27/hit 12→11/贴纸折算、自爆 −12 豁免、沉默 0；drift fire 28/41 探针+贴地封 0、extinguish 100 封顶+翻转后 −27、86+15 破百胜局、无字段人设与 2.0 行为一致）；`npm run profile-check` 15 节全绿（梯子派序 qinqi→wangyou / tanzhu→laoban→jiafang / eq 空、解锁与通关判定、胜局写账去重、复刷 90×0.3）；`npm run dom-check` 全绿（16c 连发 6 条 + 好友验证、18c 结算 +60/+110）；library/arena 无回归。生图小样实跑 2/2 成功。

---

## 13:16 ｜ M1 成长档案层落地：心池/金币/经验/结算区/演示模式（TDD 红→绿）

- **做了什么**：3.0 轨 A 的 M1 里程碑全程 TDD 落地。新增玩家成长档案层（`gang-ai:profile:v1`：等级/金币/心池/成就/称号/统计/演示模式），经济常量集中在 `ECONOMY` 表；战报屏新增「成长结算区」（经验条/金币/心变动/升级横幅/成就位，终局一次性入账、duelId 幂等防重）；大厅新增「名片卡」（Lv/金币/生命/成就占位，点击提示成就墙装修中）；选人屏新增「心池条」（5 心图标/恢复倒计时/50 币补心/心=0 全卡禁用+引导/演示模式徽章）；设置弹窗「对局」组新增演示模式开关（无限生命，即时生效）。美术轨并行：`scripts/art/generate.mjs` + 风格小样 manifest 落地，`.env.art` 真跑验证了 API 契约（中转站 503 无可用通道，待服务商侧恢复后 `--only` 重跑）。
- **动了哪些文件**：新增 `src/lib/profile.js`（档案层全部纯函数）、`src/data/achievements.js`（空表，M4 填 36 条）、`scripts/profile-check.mjs`（14 节回归）、`scripts/art/generate.mjs` + `scripts/art/manifests/style-samples.json`、`public/assets/icons/favorite.svg`（Material Symbols 官方 heart 路径）；`package.json` 加 `profile-check` script；`src/main.js` 接线（profile 状态/名片卡/心池条/结算区/settleDuel/开局 guard+duel.id/eta 定时器管理）；`src/ui/settings-dialog.js` 演示模式段控；`src/styles.css` 新组件样式（纯令牌，追加 EOF）+ `.i-favorite` + `.demo-seg` 归组；`scripts/dom-check.mjs` 增 18 节（名片卡/心池条/胜负结算/心=0 锁开局/演示模式回落）。
- **关键决策及原因**：①心恢复用「读时惰性结算」（lastRegenAt 记账，无后台定时器，离线也在回心），满血时重置记账起点防陈年余数瞬间到账；②结算幂等双保险：`duel.settlement` 挂账 + `processedDuels` 账本（封顶 50 FIFO），战报反复渲染不重算；③drift/guard 留给 M2，本阶段引擎零改动；④演示模式写档走 loadProfile/saveProfile，主屏 onChange 重读档，弹窗与主屏不共享内存对象也不脏；⑤离场时 AI 回合终局的局沿用「弃局」语义不入账（只在 finish() 路径结算），避免半路补账的复杂度。TDD 过程中抓出测试侧三处算错期望值的断言（漏算战斗币/满心优先级），实现侧零回退。
- **怎么验证的**：`npm run profile-check`（心恢复数学/结算金额/幂等/升级链/买心/坏数据收敛/成就管道 14 节全绿）；`npm run dom-check` 18 节全绿（原 17 节无回归 + 新 6 节：名片卡与存档一致、心池条状态、胜局 +60/+110、败局 心-1、连败到 0 心全卡禁用、演示模式解锁/回落）；smoke/library/arena 三项无回归；Chrome 实机过图：大厅名片卡、选人屏心形图标行（favorite.svg 200）、设置演示模式段控，控制台仅存 favicon 404（历史遗留）。生图小样 2 张因中转站 503（No available compatible accounts）未出图，脚本重试与错误处理已实证。

---

## 12:26 ｜ 3.0 立项落地：三模块立项 + 三份 designs-specs

- **做了什么**：开发者确认立项并定下轻量流程（总方案为纲、按模块直接出 designs-specs）。落地：建 `design/3.0成长系统/`、`3.0人设扩充/`、`美术升级/` 三模块目录，总方案 idea 文件随迁入成长系统目录并改状态为已立项；写三份 designs-specs（成长系统 M1 档案与经济/M2 闯关梯/M4 成就墙三阶段、人设扩充 B1 情商房 6 人/B2 杠精 +4 谈判 +3、美术升级管线+风格指南+A1-A4 清单）；建 `.env.art` 生图密钥模板并 gitignore（含 out/ 生成物目录）。
- **动了哪些文件**：新增 `docs/design/3.0成长系统/designs-specs.md`、`docs/design/3.0人设扩充/designs-specs.md`、`docs/design/美术升级/designs-specs.md`、`.env.art`；移动 `idea/3.0-大版本升级总方案.md` → `3.0成长系统/` 并更新其状态区；`docs/design/README.md` 登记三模块；`.gitignore` 加 `.env.art` 与 `out/`。
- **关键决策及原因**：spec 粒度到函数签名/常量表/验收清单（开发唯一依据）；情商房写作核心规则确认——引擎 breakdown 段是 85-100 怒气常规台词池，情商房开局 100 怒气即 breakdown 段，故**情商房 `breakdown` 数组=开场白池**（6-8 条），stages 三键按消气阶梯反向写，delta 仍按激怒量直觉填正数由引擎翻转；drift 定为「下一回合结算前按既往回合数补漂移」，保证胜局最后一击不被漂移吞掉；成就 v1 全部只发币（hearts 字段保留不填）；心=0 与生图 key 缺失都有兜底（演示模式 / 小样先行），互不阻塞三轨并行。
- **怎么验证的**：spec 中引擎事实全部现场核查——STAGES 四段区间与 breakdown 取词链（duel-engine.js:45-57、llm.js:95-99）、difficulty 仅展示用（main.js:331）、`*.local` 未盖 `.env.art` 故补 gitignore；三份 spec 与总方案数值口径一致（经济表/成就 36 条/资产 61-69 张）。

---

## 12:19 ｜ 3.0 大版本升级总方案（立项稿）

- **做了什么**：起草 2.0→3.0 大版本升级总方案，覆盖四大方向：人设扩充（每房 5-8）、房内闯关梯+生命/金币/经验成长经济、成就墙+称号、AI 生图美术升级；含决策记录、编制表、经济数值表、36 条成就清单、美术资产清单与生成管线、工程拆解、M1-M6 里程碑与三条并行轨。
- **动了哪些文件**：新增 `docs/design/idea/3.0-大版本升级总方案.md`（idea 区一页纸约定的大件立项稿）。
- **关键决策及原因**：五项决策与开发者逐项确认过——房内闯关梯（贴合「房=场景」结构）、生命制+全局心池（输局扣心/30min 回心/50 币买心/演示模式兜底）、成就产金币打通经济（复刷降档 30% 防刷）、美术全量四类走离线批量生成零运行时依赖。人设 roster 给到 13 个新增人选（含情商房 6 人解锁情商房），引擎新增 guard/drift 阻力参数把难度从数值搬到机制；现有 5 人设与 duel-engine/llm.js 零改动。
- **怎么验证的**：代码事实核查——`difficulty` 目前仅 main.js:331 选人卡星标展示（引擎未用，3.0 可接管）；版本号在 package.json（2.0.0）；待办三文档无相关旧种子，四方向均为全新立项。方案自查无占位符、经济/成就/里程碑口径一致。

