# 人设审校报告

> 范围：现有 3 个对手（杠精网友 / 阴阳怪气亲戚 / 画饼老板）的人设数据与回复台词审校，
> 外加计划书承诺但尚未实现的 2 个新人设（砍价摊主 / 甲方）的完整方案。
>
> **本报告不改 `src/` 下任何文件。** 所有修改都以「原文 → 建议」和可直接粘贴的代码块给出。
>
> 标记：**【必改】**= 是 bug，玩家能感知到；**【建议改】**= 文风/体感；**【讨论】**= 需要你拍板的设计取舍。

---

## 1. 结论摘要

按优先级排，前四条都是「现在已经坏了」而不是「还能更好」：

| # | 问题 | 位置 | 玩家感知到什么 |
|---|---|---|---|
| 1 | **SSR 称号「人形自走破防器」永远发不出来** | `titles.js:47` | 打得再好也只有 SR。这是两重原因叠在一起，见 2.2 |
| 2 | 老板三个软肋加起来 94，不是 90 | `personas.js:166,172,178` | 只点预设时别的对手停在 90 打平，老板停在 94；星级最高的反而最好打 |
| 3 | 关键词过宽，对手会答错话 | `personas.js:25,102,108` | 说「好的，那你解释一下你的逻辑」，他回你「……你怎么不接了？」 |
| 4 | 两句反应台词点名了玩家没说的词 | `personas.js:69,131` | 点第一个预设就有 1/3 概率撞上 |
| 5 | 预设即答案，「找软肋」是走过场 | 三个人设都是 | 三个按钮 = 三个软肋 = 90 分，核心玩法被自己短路 |

外加：新人设方案（§3）、补自检让「加一个对手」变安全（§4）、从 3 个对手变 5 个会踩到什么（§5）。

---

## 2. 现有三个人设审校

### 2.1 老板的软肋配平是 94，不是 90 【必改】

```
wangyou  28 + 32 + 30 = 90  ✓
qinqi    32 + 28 + 30 = 90  ✓
laoban   30 + 34 + 30 = 94  ✗
```

`duel-engine.js:18-21` 把配平规则写得很清楚：

> 数值是这么配的：每个人设三个软肋的 delta 加起来 ≈ 90，
> 所以「找齐三个软肋 + 再补一句有内容的」刚好破百；
> 而 miss 给 0，光靠废话堆到八轮也到不了 100（8 × hit 12 = 96）。
> 换句话说：不找软肋就别想赢。

`README.md:107-108` 也对玩家承诺了「只用预设不补刀的话，破防值会卡在 90 打平」。老板这里卡在 94。后果不只是数字不整齐——`demo` 的 34 是全游戏最高值，**星级最高的老板反而是三个人里最容易打穿的**，和 ★★★★ 想表达的意思正好相反。

**建议改成 32 / 28 / 30**（把最高值给 `overtime` 加班费——这是老板最有共鸣的软肋，也是他标签上写的那条）：

```js
// personas.js:166、172、178
overtime: delta: 30 → 32
demo:     delta: 34 → 28
equity:   delta: 30 → 30（不动）
```

改完顺手改掉三处硬编码的「+34」：`duel-engine.js:46`、`src/data/difficulty.js:4`、`README.md:87`。

> ⚠️ `smoke.mjs:331` 那个 `for (const t of [34, 32, 30, 12, 0, -12])` **不是**在断言老板的数据。
> 它验的是 `blendDelta(t, t) === t`——一个对任意整数都成立的恒等式，34 只是量级样本。
> 改老板不会让它失败，但它旁边的注释（`smoke.mjs:330`「老数值（软肋 32/28/30…）」）
> 列举的其实是亲戚的数值，读起来像是全量清单，容易误导。建议改成从数据推导：
> `const deltas = [...new Set(PERSONAS.flatMap(p => p.softspots.map(s => s.delta)))]`。

### 2.2 【必改】SSR 称号「人形自走破防器」永远发不出来

`titles.js:47` 读的是 `duel.softspotHits`：

```js
if (rounds <= 3 && duel.softspotHits >= 2) return byId('swift');
```

但 `createDuel()`（`duel-engine.js:164-176`）从来没有设过这个字段，它只有 `softspotKeys`：

```js
return {
  persona, personaId: persona.id, breakdown: 0, rounds: [],
  selfDestructs: 0,
  softspotKeys: [],          // ← 这一族字段只有它
  usedPresets: 0, freeTextRounds: 0, result: null,
};
```

全仓搜 `softspotHits` 只有两处命中：`titles.js:47`，和创作计划书 `:349` 里设计的数据结构。
**字段是先在计划书里设计的，引擎实现时改名成了 `softspotKeys`，`titles.js` 没跟上。**
于是 `undefined >= 2` 恒为假，SSR 分支永远不会命中。

**但把字段名改对，SSR 还是发不出来**——`rounds <= 3` 这个条件在算术上不成立。
三个不同软肋、判断分拉满到 `JUDGE_MAX = 35`，单回合上限是 `blendDelta(查表值, 35)`：

| 查表值 | 34 | 32 | 30 | 28 |
|---|---|---|---|---|
| 该回合最多涨 | 34 | 33 | 31 | 29 |

三回合最多 `34 + 31 + 31 = 96`（还是当前这位配平破了的老板），换成 32/28/30 之后统一是 `33 + 31 + 29 = 93`。
**两种配平都 < 100，所以三回合内破百在数学上不存在。**

建议改成「四轮内、三个软肋全中、其中至少两个是自己打的字」：

```js
// titles.js
import { uniqueSoftspotHits } from '../lib/duel-engine.js';   // 无循环依赖：duel-engine 不 import titles

if (rounds <= 4 && uniqueSoftspotHits(duel) >= 3 && duel.freeTextRounds >= 2) return byId('swift');
```

同时把 `titles.js:7` 的描述从「三轮之内让对手当场闭麦」改成「**四轮之内**让对手当场闭麦……而且三个软肋是你自己找出来的，不是照着按钮念的」。

这样标准演示路径（3 个预设 + 1 句自己的话）落在 SR「阴阳大师」，SSR 留给真正自己找软肋的人——`freeTextRounds` 的定义在 `duel-engine.js:212`，预设和沉默都不计入。

顺手可以删掉 `titles.js:48-50` 的冗余分支（`rounds <= 5` 和 `> 5` 都 `return byId('master')`，两支返回值一样）。

> 已知小口子：把预设句子原样手打出来也算自由文本。demo 里无所谓，但如果你想堵死，
> 判据可以换成「命中软肋的回合里有几轮 `usedPreset === false`」。

### 2.3 关键词过宽，对手会答错话 【必改】

`matchSoftspot`（`duel-engine.js:108-115`）是**先命中先赢**，按 `softspots` 的声明顺序：

```js
for (const spot of persona.softspots) {
  if (spot.keywords.some((kw) => t.includes(kw))) return spot;
}
```

一个过宽的词会盖住后面那个更准的软肋。实测（`node` 直接调 `matchSoftspot` 验证）：

| 位置 | 过宽的词 | 玩家说 | 现在判定 | 应该判定 |
|---|---|---|---|---|
| `personas.js:25` | `'好的'` | `好的，那你解释一下你的逻辑` | `shutup` +28 → 他回「……你怎么不接了？你这样很没意思。」 | `logic` +30 |
| `personas.js:102` | `'我请'` | `我请问你凭什么` | `hongbao` 塞红包 +28 | 有效输出 |
| `personas.js:102` | `'给您'` | `给您添麻烦了` | `hongbao` +28 | 无效输出 |
| `personas.js:108` | `'您来'` | `您来我家吃饭` | `demo` +30 | —— |

第一行是最难看的：玩家明明**接得更紧了**，杠精却问他为什么不接。因为 `shutup` 是 `wangyou.softspots[0]`，`logic` 是 `[2]`，前面的把后面的遮住了。

**两层修法**：

1. 收窄过宽的词：`'好的'` → `'好的好的'`；`'我请'` → `'我请客'`；`'给您'` → `'给您红包'` / `'给您钱'`；`'您来'` → `'您来一个'`。
2. 把声明顺序改成**窄的在前、宽的在后**——`shutup` 挪到 `logic` 和 `screenshot` 后面。

第 2 步是安全的：没有任何地方把 `presets` 和 `softspots` 按下标对齐（`main.js:279` 只遍历 `presets`，`record.softspotKey` 和 `softspotReactions[spot.key]` 全按 key 走），9 个预设也没有一个同时命中两个软肋，所以调序不会改变任何预设的判定结果。唯一会变的是**自由文本在歧义时落到哪个软肋**（连带 delta 和反应台词池）——这正是我们想要的。

### 2.4 反应台词里写死了关键词 【分两级】

判据是：**玩家最自然的说法（尤其是预设本身）会不会触发一句对不上词的台词？**

**严重——预设自己就会触发**（点第一个预设就有 1/3 概率撞上，因为 `pick()` 是随机取一条）：

| 位置 | 原文 | 触发它的预设 | 建议 |
|---|---|---|---|
| `personas.js:69` | `'我这怎么就偷换概念了……'` | `'你这逻辑前后矛盾啊'`（没有一个字提到偷换概念） | `'我、我这不是那个意思……'` |
| `personas.js:131` | `'你问他干嘛，他还在考呢。'` | `'您家孩子现在在哪儿高就啊？'`（只有「考得怎么样」才对得上「考」） | `'你问他干嘛，他自己的事还没弄明白呢。'` |

**中等——次要关键词对不上**（主关键词和预设都对得上，只有走冷门词时串味）：

- `personas.js:64,65`「截图」：关键词里还有 `挂了 / 发群里 / 发微博 / 曝光 / 转发`
  → 建议 `'你……你别乱来啊。'`、`'不是，你想发哪去？'`
- `personas.js:135-137` 三条都假设钱已经递过去了：碰到 `'我请你吃饭'` 时「行了行了，收回去」很怪
  → 建议第 3 条改 `'你这孩子，又来这套。'`（去掉「收回去」这个动作）

**轻微——主关键词就是标签本身**（`overtime` 标签写着「追问加班费」，说到别的词串味概率低）：
`personas.js:201`、`personas.js:211` 可以不改。要改的话去掉名词即可（`'这个……是要走流程的，流程你懂吧。'`）。

**原则**：反应应该回应「我哪儿被戳中了」，而不是复述玩家可能没说的那个词。
反例里做得最好的是 `laoban.softspotReactions.demo`（`'我……我这不是忙嘛。'`）——不管玩家说的是「示范」还是「您带头」，这句都成立。

### 2.5 文风与体感 【建议改】

**a) 三个人的 `breakdown[0]` 都是 `'……'`**（`personas.js:75,146,217`）。
破防是全游戏的情绪顶点（`main.js:508` 把整个 `breakdown` 数组当作结束气泡连发），
三个人开口一模一样，等于把这个瞬间浪费了。建议每人一个专属姿态：

| 人设 | 现在 | 建议 |
|---|---|---|
| 杠精网友 | `'……'` | `'（对方正在输入……）'` |
| 阴阳怪气亲戚 | `'……'` | `'……'`（她这句最好，保留） |
| 画饼老板 | `'……'` | `'（他把那杯美式放桌上了）'` |

后两个直接用上了各自的 `intro` 道具（那杯没喝完的美式），比一个省略号有画面得多。

**b) 老板的台词最泛。** 亲戚有「隔壁小王，人家都买房了」和红包，网友有截图和逻辑，老板全是「格局 / 年轻人 / 层次」的自我复读——`stages.polite` 三条基本是同一个笑话说三遍。建议换成具体道具：期权池、团建、周报、OKR、「我当年是从大厂出来的」。

**c) `demo` 这个软肋在两个角色身上重复。** 亲戚和老板都是「请您先做示范」，key 同名、套路相同。而且 `laoban.softspotReactions.demo[1]`（`personas.js:207`）是「你让我做？我要能做还要你干嘛」——和我给甲方设计的 `reference[0]`「我要是知道我想要什么，还要你干嘛？」是同一个包袱。上级/客户这两个角色共用一个梗，读起来像偷懒。建议老板那条改成「我现在这个位置，你觉得我还有时间做这个？」

**d) 【讨论】预设即答案。** 三个预设正好是三个软肋，点完就是 90，再补一句话就赢——「找软肋」这个核心玩法被自己的 UI 短路了。你在计划里选的是**只给最硬的甲方留一个诱饵**，现有三个人设和全部测试原样不动，这样 ★★★★★ 才是真难。

**e) 星级目前是装饰。** 三个人设靠预设都一样好打，★★★ / ★★ / ★★★★ 实际只反映「不看预设能不能猜到软肋」。加上甲方的诱饵之后，这一栏终于对一个人有了实际含义。

---

## 3. 新人设方案

计划书 §2 承诺的是「首批 5 个人设」，代码里只有 3 个。下面补齐缺的两个，招牌台词沿用计划书原句。

数据已按现有结构写全 13 个字段（`id / name / avatar / difficulty / tagline / catchphrases / intro / opener / presets / softspots / stages / softspotReactions / breakdown`），并且已经跑过校验：delta 合计 90、`softspotReactions` 的 key 与 `softspots` 一一对应、每个预设解析到预期的 `hitType`、诱饵必须是 `miss`、同人设内无重复关键词。

### 3.1 砍价摊主（★★）

设计要点：三个软肋全是真软肋，**这个角色保持「点完预设 90 分」的友好路径**——他是新手第一个该碰到的对手。
最得意的一条在 `scale` 里：`'……要不我再给你搭把葱。'`——中式摊主式投降。

```js
{
  id: 'tanzhu',
  name: '砍价摊主',
  avatar: '🥬',
  difficulty: 2,
  tagline: '这个价我进货都进不来',
  catchphrases: ['你再加点', '真的是成本价了', '我这一天白干了'],
  intro: '早市最后一排，菜叶上还挂着水。他的秤盘永远往上翘那么一点点，你还没开口，他先说「随便看」。',
  opener: '这个价我进货都进不来，你再加点。',
  presets: ['那我不买了，我再转转', '您这秤准吗？', '进货单给我看看呗'],
  softspots: [
    {
      key: 'walkaway',
      label: '转身就走 / 说去别家',
      delta: 32,
      keywords: ['不买了', '我再转转', '上别家', '隔壁那家', '那我就不要了', '别家才卖', '算了'],
    },
    {
      key: 'scale',
      label: '质疑他的秤',
      delta: 28,
      keywords: ['秤', '缺斤少两', '分量', '够不够', '复秤', '称一下', '掂量', '是不是少了'],
    },
    {
      key: 'cost',
      label: '要看进货价',
      delta: 30,
      keywords: ['进货', '进价', '成本', '批发价', '上货', '单据', '发票', '拿货'],
    },
  ],
  stages: {
    polite: [
      '随便看随便挑，都是今早刚到的。',
      '你要多少？多要点我给你算便宜些。',
      '这个价我真不赚钱，就挣你两毛钱。',
    ],
    sarcastic: [
      '哎哟，你这价我在旁边听着都想笑。',
      '你再去别家问问，问完你就回来了。',
      '行行行，你慢慢挑，反正我这一天也不忙。',
    ],
    agitated: [
      '你这不是砍价，你这是抢。',
      '我在这卖了二十年菜，没见过你这样的。',
      '别在这耽误我做生意，后面还有人呢。',
    ],
  },
  softspotReactions: {
    walkaway: [
      '哎哎哎……回来回来，好商量。',
      '你别走啊，价钱好说！',
      '……行了行了，那个价给你，我认了。',
    ],
    scale: [
      '秤？我这秤每天早上都校过的！',
      '你、你这话说的，我在这卖二十年了。',
      '……要不我再给你搭把葱。',
    ],
    cost: [
      '进货单？那、那是我自己的事吧。',
      '你管我进价多少，反正我卖你就这个价。',
      '……行了，成本价给你，别说了别说了。',
    ],
  },
  breakdown: [
    '不是，你、你这人怎么这样啊……',
    '拿走吧拿走吧，今天算我倒霉。',
    '收摊了收摊了。（对方已收摊）',
  ],
},
```

### 3.2 甲方（★★★★★，全游戏最难）

设计要点：**3 个预设里只有 2 个是真软肋，第 3 个是诱饵。**

```
这个需求写进合同了吗？   → contract  +30  ✓
预算是多少？             → budget    +28  ✓
我这就去改，您别生气     → miss        0  ← 诱饵
```

诱饵是整个角色最好笑的地方：**秒答应就是甲方最喜欢的反应，等于自愿加班。** 他高高兴兴地回你一句，而你白扔一个回合——这个教训比任何台词都清楚。

于是他的获胜路径变成四回合：2 个预设（`30 + 28 = 58`）→ 自己找出第三个软肋 `reference`（`+32 → 90`）→ 再补一句有效输出（`+12 → 102`，被 `MAX_BREAKDOWN` 夹到 100）。而且这条路自由文本占两轮以上，正好够到 §2.2 里 SSR 的新门槛——**「打通甲方并且自己找软肋」就是拿到 SSR 的自然路径**。

金币句是 `reference` 的反应：`'我要是知道我想要什么，还要你干嘛？'`

```js
{
  id: 'jiafang',
  name: '甲方',
  avatar: '📑',
  difficulty: 5,
  tagline: '我要五彩斑斓的黑',
  catchphrases: ['这个需求很简单', '你先做一版我看看', '感觉不对，再调调'],
  intro: '永远在周五下午五点五十九分发来一句「在吗」，附件是一个叫「最终版(3)」的文件。',
  opener: '在吗？有个小需求，很简单，你今天能给我吗？',
  presets: ['这个需求写进合同了吗？', '预算是多少？', '我这就去改，您别生气'],
  softspots: [
    {
      key: 'contract',
      label: '要合同 / 验收标准',
      delta: 30,
      keywords: ['合同', '写进合同', '需求变更', '验收标准', '验收', '需求范围', '确认单', '邮件确认', '签字'],
    },
    {
      key: 'reference',
      label: '让他自己给参考',
      delta: 32,
      keywords: ['参考', '您先给', '您找个', '样例', '对标', '您想要什么样', '您给我个方向', '设计稿', '竞品'],
    },
    {
      key: 'budget',
      label: '问预算',
      delta: 28,
      keywords: ['预算', '加钱', '报价', '费用', '多少钱', '经费', '付款', '结款', '尾款'],
    },
  ],
  stages: {
    polite: [
      '这个需求很简单的，你先做一版我看看。',
      '不用太复杂，就要那种高级感。',
      '你们是专业的，你懂的比我多。',
    ],
    sarcastic: [
      '我花了这么多钱，这点事都做不好？',
      '你先做，做出来我就知道我想要什么了。',
      '别人家都能做，怎么就你不行？',
    ],
    agitated: [
      '我不管过程，我只要结果。',
      '这是你们的问题，不是我的问题。',
      '我很失望，真的很失望。',
    ],
  },
  softspotReactions: {
    contract: [
      '合同……合同上是这么写的吗？',
      '哎呀，都是自己人，走什么流程。',
      '……行，那我发个邮件确认一下。',
    ],
    reference: [
      '我要是知道我想要什么，还要你干嘛？',
      '参考？你才是专业的啊。',
      '……行吧，我找两张图给你。',
    ],
    budget: [
      '预算这个……你先做，做完我们再谈钱。',
      '你先垫一下，公司还能差你这点钱？',
      '……这个我跟财务申请一下。',
    ],
  },
  breakdown: [
    '不是，你这个态度……',
    '行，这个事我让法务跟你对接。',
    '（已读不回）',
  ],
},
```

### 3.3 诱饵的两种实现 【讨论】

诱饵点下去是 `miss`，本地引擎会从 `stages[stage.id]` 取一句通用台词——**惩罚到位，但不好笑**。想让它好笑（他回你一句「对对对，就是这个感觉！」）需要给预设开一个口子：

```js
// llm.js 的 localTurn 里，判定之后、取 stages 之前加一句（约 5 行）
// 数据侧：persona.presetReplies = { '我这就去改，您别生气': '对对对，就是这个感觉！你先改着，我这边再想想。' }
const presetLine = turn.usedPreset ? persona.presetReplies?.[turn.userText] : null;
```

代价是动 `llm.js`。**我推荐这一版**——诱饵的全部价值就在那个笑点上；但如果想先看效果，零改动版也能玩，只是笑点少了。

---

## 4. 补自检：让「加一个对手」变安全

`scripts/smoke.mjs:73-82` 那段「关键词自检」**只 `console.log`，从来不调 `fail()`**：

```js
for (const p of PERSONAS) {
  const results = [];
  for (const text of p.presets) {
    const t = await generateTurn({ persona: p, duel, userText: text });
    results.push(`${text} ${t.hitType === 'softspot' ? '✓' : `✗(${t.hitType})`}`);
  }
  console.log(`关键词自检 ${p.name}: ${results.join(' | ')}`);
}
```

关键词打错了、预设命不中软肋，它会打印一行 `✗` 然后**测试照样全绿退出 0**。它恰好就是能拦住 §2.3 那类问题的地方。两处要改：

1. **换成同步的 `localHitType` / `matchSoftspot`。** 现在每条预设都会走一次 `generateTurn`，纯 node 下要 `sleep(350~800ms)`（`llm.js:475`），而且结果**取决于当时有没有配 key**——那一刻 `localStorage` 还没被 stub，只是碰巧走了本地。
2. **补 `fail()`，并把校验扩成一组不变量。**

建议断言（凡是能从 `STAGES`、`TABLE_DELTA` 推导的就别写死）：

- ★ **`1 <= difficulty <= 5` 且为整数。** `main.js:192` 是 `'★'.repeat(difficulty) + '☆'.repeat(5 - difficulty)`，而 `String.prototype.repeat` 对负数会抛 `RangeError`——**星级写错一个数字就能让选人屏白屏**。全清单里最值钱的一条。
- ★ **三个软肋的 delta 合计落在 `[88, 99]`**：`< 100` 才能保证「只点预设打平」，`+12 >= 100` 才能保证「找齐三个再补一句」赢。再单独断言一条「**等于 90**」作为 demo 配平，失败信息里写明为什么是 90（否则下一个人只会觉得这数字很怪）。
- ★ **每个 `softspots[].key` 都有非空的 `softspotReactions[key]`。** 缺了会静默降级到 `stages[stage.id]`（`llm.js:95` 的 `||`），完全看不出问题。
- ★ **同一个人的两个软肋不能共用一个关键词**（跨人设可以——`qinqi` 和 `laoban` 都有 `demo`，匹配是按人设走的）。
- **每个预设经 `localHitType` 都解析成 `softspot`，且三个预设命中三个不同的软肋。** 同一个软肋连点三次会触发减半（`duel-engine.js:195`），合计只剩 `2d`，90 分的配平直接崩。（甲方有诱饵，所以这条要放宽成「至少 2 个预设命中软肋，且命中的互不相同」——或者按人设声明预期值。）
- **`polite` / `sarcastic` / `agitated` 三个键都存在且非空**（键名从 `STAGES` 里排除 `breakdown` 推导）；`breakdown`、`opener`、`tagline`、`intro`、`catchphrases`、`avatar` 都非空（`main.js:508` 遍历 `breakdown`，`llm.js:150` 做 `catchphrases.join`，缺了就是崩）。
- **`id` 全局唯一**（`getPersona` 返回第一个匹配，重复 id 会让后面那个人设永远选不到）。
- 软肋数量断言成 **`>= 2`** 而不是 `=== 3`——`README.md:8` 写的就是「2–3 个软肋」。

再补一张**复现句探针表**，把 §2.3 那类「单个词看着合理、行为是错的」钉死：

```js
// [人设 id, 玩家说, 期望的软肋 key（null = 不该算软肋）]
const PROBES = [
  ['wangyou', '好的，那你解释一下你的逻辑', 'logic'],   // 不是 shutup
  ['qinqi',   '我请问你凭什么',             null],
  ['qinqi',   '给您添麻烦了',               null],
  ['jiafang', '我这就去改，您别生气',        null],   // 诱饵
];
```

这类 bug 没有任何通用断言能抓住（`'好的'`、`'我请'` 单独看都是合理的关键词，错的是行为），探针表是唯一的防线，顺便也是给下一个写人设的人留的说明书。

---

## 5. 从 3 个对手变成 5 个：会踩到什么

- **`dom-check.mjs:28` 会直接失败**：`if (cards.length !== 3) fail(...)`。
  正确改法是 `import { PERSONAS } from '../src/data/personas.js'`（纯数据、无 DOM 依赖，node 下安全）然后比对 `PERSONAS.length`——**别把字面量改成 5**，那只是把同一个 bug 推到第 6 个角色。
- **`dom-check.mjs:61` 是静默耦合，比上一条更危险**：

  ```js
  cards[1].click(); // 阴阳怪气亲戚
  ```

  索引 1 是亲戚**只是因为当前数组顺序**，紧接着 `:144` 断言破防值 90（那是亲戚的数据）。
  **新角色只能往 `PERSONAS` 末尾追加**；插到前面会让它去跟错误的人设对线，而失败信息
  （「三个软肋应该到 90，实际 N」）会把你带偏。建议改成按名字找卡，或在 `main.js:174-181`
  给卡片加个 `data-persona` 属性——这个改动本身也值得做，它让测试不再依赖数组顺序。
- **不受影响的计数**（我逐个确认过，都是别的维度）：`dom-check.mjs:73`（预设数 3）、`:49`（难度数 3）、`:180`（服务商数 3）、`:86`（表情数下限）。只有 `:28` 真的要改。
- **CSS**：`.persona-grid`（`styles.css:224-227`）没有 `grid-template-columns`，是单列堆叠。5 张卡不会撑坏布局（`.select` 本来就能滚），只是要多滚一会儿。想要桌面端两列得新加 `grid-template-columns: repeat(auto-fill, minmax(260px, 1fr))`——但全仓只有一个 `max-width: 560px` 查询、没有 min-width 的先例，属于新写法，看你取舍。
- **文档**：`README.md:118` 的「personas.js　3 个人设」要改成 5；`README.md:140`「加一个对手只需要往 `src/data/personas.js` 里加一条」在 §4 的自检补完之后才真正成立（今天它是句空话——真加一条会让 `dom-check` 挂掉）。

---

## 6. 落地清单

按依赖顺序排，每步都可独立验证：

| 步 | 改什么 | 文件 | 验证 |
|---|---|---|---|
| 1 | 修 `softspotHits` + 放宽 `rounds <= 4` + 改称号描述 | `titles.js:6,47-50` | 手打一句自己的话赢一局，看报告页称号 |
| 2 | 老板 delta 改 32/28/30 + 三处「+34」注释 | `personas.js:166,172,178`、`duel-engine.js:46`、`difficulty.js:4`、`README.md:87` | 只点老板的三个预设，应停在 90 |
| 3 | 收窄 4 个过宽关键词 + 调 `wangyou.softspots` 顺序 | `personas.js:20-39,102,108` | 跑 §4 的探针表 |
| 4 | 改 2 条严重 + 2 条中等的反应台词 | `personas.js:69,131,64,65,137` | 点第一个预设多次，看有没有对不上词的 |
| 5 | 三个人设的 `breakdown[0]` 差异化 | `personas.js:75,217` | 赢一局看结束气泡 |
| 6 | 追加两个新人设 | `personas.js`（数组末尾） | `npm run smoke` |
| 7 | 「关键词自检」改硬断言 + 探针表 | `smoke.mjs:73-82` | 故意改坏一个关键词，测试必须红 |
| 8 | 卡片数断言改成 `PERSONAS.length` | `dom-check.mjs:28` | `npm run dom-check` |
| 9 | 诱饵笑点（可选，动 `llm.js`） | `llm.js` `localTurn` | 点甲方第三个预设，看他高高兴兴回你一句 |
| 10 | README：人设数、+28~32、甲方诱饵说明 | `README.md:87,95,118` | 通读 |

做完 6 和 7 之后，「加一个对手只需要往 `personas.js` 里加一条」这句话才真正成立。
